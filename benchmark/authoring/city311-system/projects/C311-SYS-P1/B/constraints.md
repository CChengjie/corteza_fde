# Constraints

The B source is an empty launch harness, not Corteza or City311. The answer
must remain independently runnable from an empty database and may not embed
credentials in its image, response, or logs.
