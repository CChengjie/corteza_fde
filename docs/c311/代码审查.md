# C311 代码审查

---

## 2026-09-08 20:44

## FE-09 阻断修复后复审（Frontend Mock-only）

### 审查目标

- 类型：本地 `FE-09` 分支当前工作区复审。
- 基线：HEAD `c6881d4`，分支与 `origin/2024.9.x` 一致；工作区包含未提交的 FE-09 前端、Mock、测试和审查记录改动。
- 范围：工作流、OAuth action、ICS 日历、邮件、报告、审计、联系人邮件导出和 CRM 数据导出，以及对应的 provider、Mock、types、fixtures、路由、页面和测试。
- 边界：所有前端能力按 Mock-only 审核；不要求真实 API，不修改后端、数据库或冻结契约。Mock 仍必须遵守冻结契约和技术方案的字段、权限、错误及状态语义。

### 自动化检查与复测证据

- `corepack yarn test:unit`（`lib/js`）：`269 passing, 16 pending`。
- `corepack yarn test:unit`（`lib/vue`）：`53 passing`。
- `corepack yarn test:c311 --runInBand`（Admin）：`95 passing`；仅有既有 jsdom navigation warning。
- `corepack yarn tsc --noEmit`（`lib/js`）：通过。
- `lib/js`、`lib/vue` 生产构建：通过；仅有既有 Sass/Browserslist/Leaflet 资源告警。
- `eslint src/views/C311/Extensions.vue src/views/routes.js`（Admin）：通过。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_fe09_matrix.py'`：`2` 项通过。
- `tools/c311-browser/run-fe09.sh`：`44/44` 通过，覆盖 4 个角色及 `1440x900`、`390x844`；无页面错误、横向溢出或未预期请求。
- `git diff --check`：通过；变更文件未包含 `server/`、数据库或迁移。
- 独立 provider 复测：报告日期范围返回 422；terminal 工作流测试的 operation 为 `FAILED`，但新增 execution log 仍为 `SUCCEEDED`；未知 operation ID 返回合成 `SUCCEEDED` 而非 404；6 MiB 邮件附件被接受；含脚本的邮件 HTML 返回 422 而不是清理后的预览。

### 已确认关闭

- CRM 导出按实体和当前记录范围过滤，opaque page token 已绑定实体、过滤、更新时间、排序和 page size。
- 报告导出已限制为 `CSV`；保存报告已建模 owner，分享范围受创建者范围限制。
- 审计过滤控件、审计 CSV 字段、联系人 verified email/退订过滤、CRM 四类实体导出和 429 `Retry-After: 60` 已接入 Mock。
- 工作流 OAuth 专用错误、ICS 同 UID 导入摘要、邮件幂等发送和投递状态查询已有成功路径及基础错误测试。

### 🔴 严重问题

1. **五类最小报告没有可观察的必需结果字段，且日期范围过滤不可用。**
   - 位置：`lib/js/src/311/mock-provider.ts:248-252,1912-1930,1934-2005`。
   - 问题：报告目录虽列出五项，但 `reportRows()` 对 `service_requests` 只返回原始请求，对 `constituents` 只补 email/department/district，对 `follow_up_actions` 只返回 action 原字段；没有生成请求状态年龄、协作者/活动请求/逾期提醒计数、解决耗时/重新打开标志、请求编号/部门等五类报告要求的派生字段。`created_from`/`created_to` 也不在实体支持过滤器中，提交即返回 422。
   - 风险：报告目录和构建器无法证明技术方案 §9.8.1-§9.8.2、§9.8.6，用户保存的报告与最小报告结果不一致。
   - 修复：为五类最小报告建立确定性 fixture/派生投影，按定义应用日期范围及词汇过滤、分组和排序，并让 operation 结果/CSV 只返回所选字段；补充每类报告的结果字段和日期范围测试。

2. **审计空结果时活动筛选器被隐藏。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:531-631,738-741`。
   - 问题：筛选表单位于 `c311-data-state` 的 `populated` slot，`state === 'empty'` 时只渲染 `No data.`，筛选控件和当前值消失。
   - 风险：违反 FE-09 “空结果必须保留活动过滤器”，用户无法调整筛选条件或复查查询上下文。
   - 修复：将筛选器移到数据状态组件外，或在 `empty` slot 复用同一筛选器，并增加 empty + refresh 浏览器断言。

3. **扩展页面没有使用统一的 HTTP 错误状态映射。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:771`。
   - 问题：加载错误仅按 `retryable` 映射为 `retryable-error`/`terminal-error`，401、403、404、422 未调用已有 `c311StateForError()`。
   - 风险：身份失败、权限拒绝、资源不存在和校验错误都会显示成通用终止错误，路由/数据状态与契约语义不一致。
   - 修复：复用 `c311StateForError(error)`，并为 401/403/404/422 增加页面断言。

4. **UTC ICS 事件的更新/取消会生成非法 `TZID` + `Z`。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:794-798`。
   - 问题：`calendarImportBody()` 无条件补 `TZID=America/New_York`；事件原值如 `20260115T150000Z` 会生成 `DTSTART;TZID=America/New_York:20260115T150000Z`，provider 会拒绝该组合。
   - 风险：冻结 §7.4.3 要求的 UTC `Z` 事件无法通过同 UID 更新/取消，日历链路在真实交换时失败。
   - 修复：UTC 值保持无 `TZID` 并保留 `Z`；本地值才使用 `TZID=America/New_York`，增加 UTC 更新和取消测试。

5. **邮件富 HTML 没有编辑入口，也没有按 §11.5.7 白名单清理。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:379-419,785-790`、`lib/js/src/311/mock-provider.ts:2200-2222`。
   - 问题：页面只编辑纯文本，预览用 `<pre>` 显示 HTML 源码；Mock 对脚本/事件属性等危险内容直接返回 422，没有生成仅含 `p, br, strong, em, ul, ol, li, a, table, thead, tbody, tr, th, td` 及允许属性的清理结果。
   - 风险：FE-09 的富文本模板/预览功能不可用，且无法证明存储/渲染前的清理闭环；切换真实 API 时会产生明显 parity 差异。
   - 修复：增加受控 HTML 编辑/预览，Mock 返回白名单清理后的 HTML；禁止危险元素、属性和协议，并补清理结果断言。真实 HTML 安全边界仍由后端负责，不能在 Mock 通过后宣称生产安全。

6. **邮件附件沿用门户 10 MiB 上限，违反邮件 5 MiB 限制。**
   - 位置：`lib/js/src/311/mock-provider.ts:2216-2218`、`lib/js/src/311/provider.ts:215-223`、`lib/js/src/311/enums.ts:100`。
   - 问题：`sendMail()` 复用 `validatePortalAttachment()`，上限为 10 MiB；独立复测 6 MiB 邮件附件可通过。
   - 风险：Mock 接受真实契约应拒绝的邮件，可能在联调或投递前造成大小限制不一致。
   - 修复：为邮件附件增加独立 5 MiB 校验，保留最多 3 个及契约媒体类型，补 5 MiB 边界测试。

7. **工作流列表路由错误要求 `workflow.execute` scope。**
   - 位置：`client/web/admin/src/views/routes.js:110-114`、`client/web/admin/src/views/C311/Staff.vue:1509`。
   - 问题：`workflow_list` 契约仅声明 capability；`workflow.execute` 只属于 `/api/v1/actions`。当前工作流列表路由同时声明 `capabilities: ['workflow_list']` 和 `scopes: ['workflow.execute']`，合法的 capability-only 角色会被错误重定向 403。
   - 风险：平台管理员/被授予工作流列表能力但未授予 OAuth 执行 scope 的会话无法访问工作流管理页面，能力显示与路由行为不一致。
   - 修复：工作流管理路由仅按 `workflow_list` capability 和 route grant 守卫；OAuth action 单独按 `workflow.execute` scope，补 capability-only 角色测试。

8. **terminal 工作流测试的执行日志与 operation 结果不一致。**
   - 位置：`lib/js/src/311/mock-provider.ts:2067-2077`。
   - 问题：`scenario='terminal'` 时 operation 查询返回 `FAILED`，但 `testWorkflow()` 仍把 execution 写成 `outcome: 'SUCCEEDED', succeeded: true`。
   - 风险：用户可同时看到失败 operation 和成功执行日志，违反执行日志必须记录真实评估/动作成功或失败的要求。
   - 修复：根据最终 operation 结果写入失败 execution、错误详情和 `succeeded: false`；补 terminal 日志与 operation 一致性断言。

9. **邮件 SMTP 421/451/550/553 场景、重试次数和终止记录没有 fixture 或 E2E 证据。**
   - 位置：`lib/js/src/311/enums.ts:202-263`、`lib/js/src/311/fixtures.ts:404-408`、`tools/c311-browser/fe09_matrix.py:188-205`。
   - 问题：仅有通用 `retryable`/`terminal` 场景，未声明四个 SMTP 响应码或对应 fixture；现有浏览器测试只验证一次通用轮询成功/失败。
   - 风险：无法证明 §10.6.1-§10.6.2 要求的 421/451 可重试、550/553 永久失败、最多三次尝试及终止记录，后续真实邮件联调会缺少可比对的 Mock 证据。
   - 修复：新增四个 SMTP code fixture，明确 1 秒/5 秒重试状态与最多三次尝试，550/553 进入 `TERMINAL_FAILURE` 并保留错误记录；补 provider、组件和浏览器断言。

10. **未知 operation ID 被合成为成功。**
    - 位置：`lib/js/src/311/mock-provider.ts:1124-1150`。
    - 问题：`getOperation()` 找不到 ID 时返回 `SUCCEEDED`、空 result；冻结 `operation_get` 要求未知 ID 返回 404。
    - 风险：错误的 operation ID 会被前端当成成功，掩盖轮询/路由/持久化错误，并与真实 API 行为不一致。
    - 修复：未知 ID 返回契约规定的 `NOT_FOUND` 404，补未知 operation 测试。

### 🟡 非阻塞风险

- `Extensions.vue:776` 保存已有工作流时固定发送 `active: false`。虽然当前 Mock 的 `updateWorkflow()` 保留 active 值，真实 API 可能把编辑已激活流程误停用；建议保存时回传当前 active，激活/停用仅由显式操作完成。
- `Extensions.vue:772` 最多只调用一次 `getOperation()`；FE-11 真实 API parity 前应补 `PENDING/RUNNING` 轮询、退避、取消和刷新恢复策略。
- 当前 FE-09 浏览器矩阵只覆盖 `1440x900`、`390x844` 和 Chromium；360、767、768、1023、1024、1920 及 Edge/Firefox/Safari 属于 FE-10，但提交记录应明确范围。
- HTTP provider 的 OAuth/data export `Authorization` parity 留到 FE-11，不应在 Mock-only 阶段把秘密写入前端。

### 结论

**Request Changes：FE-09 尚未完成，不能提交或合入。** 当前有 10 项 P1 阻断。自动化测试和 44/44 浏览器矩阵通过，只能说明现有 happy path 的 Mock 自洽，不能覆盖上述契约边界。请仅修改前端 Mock/provider/types/fixtures/UI/test，关闭全部阻断后重新运行单元、组件、定向 lint、TypeScript、构建、FE-09 浏览器矩阵和 `git diff --check`；无需等待或修改后端、数据库或冻结契约。

---

## 2026-09-08 18:47

## FE-09 再次复审（Frontend Mock-only）

### 审查目标

- 类型：本地 `FE-09` 分支工作区复审；HEAD `c6881d4`，与 `origin/2024.9.x` 一致。
- 范围：工作流、OAuth action、ICS、邮件、报告、审计、联系人邮件导出和 CRM 数据导出，以及对应 provider/mock/types/fixtures/UI/测试。
- 边界：所有前端能力按 Mock-only 审核；不要求真实 API，不修改后端、数据库或契约。Mock 仍必须遵守冻结契约的权限、分页、字段和错误语义。

### 自动化与复现证据

- `corepack yarn test:unit`（lib/js）：`265 passing, 16 pending`。
- `corepack yarn test:unit`（lib/vue）：`53 passing`。
- `corepack yarn test:c311 --runInBand`（Admin）：`94 passing`。
- lib/js TypeScript、lib/js/lib/vue/Admin 构建、FE-09 定向 lint、FE-09 Python 静态测试：通过。
- `tools/c311-browser/run-fe09.sh`：`44/44` 通过，覆盖 4 个角色和 `1440x900`/`390x844`；无页面错误、横向溢出或未预期请求。
- `git diff --check`：通过；工作区包含未提交 FE-09 改动，未发现 `server/`、数据库或迁移文件改动。
- 直接调用 Mock provider 的契约边界复现仍失败（见下列阻断）。

### 已确认关闭

- 工作流定义/执行日志/版本恢复及 OAuth `INVALID_CLIENT`、`INVALID_TOKEN`、`INSUFFICIENT_SCOPE` 场景。
- ICS 同 UID 更新、取消、未知 UID 忽略、元数据保留和刷新恢复。
- 邮件模板、输入校验、清理预览、幂等发送和投递状态查询。
- 审计范围与完整 CSV 字段、联系人 verified email/退订过滤、四类 CRM 导出实体投影。

### 🔴 严重问题（仍阻断）

1. **CRM 数据导出 opaque page token 未绑定原查询。**
   - 位置：`lib/js/src/311/mock-provider.ts:2387,2465-2468`。
   - 复现：第一页 `page_size=1` 返回 `r2` 和 `opaque-next`；携带该 token 改用 `page_size=2` 返回 `r4`、`request-fixture-001`，跳过 `r3`。改变 `filters` 或 `updated_since` 也会被接受。
   - 风险：分页漏记录/重复记录，违反 FE-09 的稳定分页与 token binding 要求。
   - 修复：在 Mock 中保存并校验 entity、filters、updated_since、游标及分页上下文；不匹配返回契约规定的 `INVALID_PAGE_TOKEN`/400，并补测试。

2. **报告导出未校验 `format=CSV`。**
   - 位置：`lib/js/src/311/mock-provider.ts:1926-1940`。
   - 复现：`exportReport('report-fixture-001', { format: 'JSON' as any })` 仍返回 `PENDING` operation；契约 `report_export` 要求 `format` 为常量 `CSV`。
   - 风险：Mock 接受真实 API 应拒绝的请求，产生 mock/real parity 偏差。
   - 修复：创建 operation 前只接受 `CSV`，其他值返回 `VALIDATION_ERROR`/422，且不产生写入。

3. **保存报告未建模 `resource_owner`。**
   - 位置：`lib/js/src/311/mock-provider.ts:1875-1887,2255-2277`、`lib/js/src/311/fixtures.ts:376-389`。
   - 复现：默认报告没有所有者，`service_agent`、`supervisor`、`department_manager`、`workflow_designer` 都能调用 `updateReport` 成功；实现无法证明契约要求的 owner 授权。
   - 风险：非所有者可修改/共享报告，可能扩大数据范围。
   - 修复：在 fixture metadata 中声明 owner；更新和共享按 owner 授权，平台管理员按契约处理；共享范围按创建者范围计算并补非 owner 测试。

4. **OAuth 导航未按 `workflow.execute` scope 隐藏。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:689`；路由守卫 `client/web/admin/src/views/routes.js:134-137` 已检查 scope。
   - 问题：导航项没有 `scope` 字段，无该 scope 的员工仍看到 OAuth 链接，点击后才被重定向 403，违反 capability/scope 驱动显示。
   - 修复：导航项声明 `scope: 'workflow.execute'`，补无 scope 角色的导航隐藏和直接 URL 403 断言。

5. **审计过滤 UI 和 page token 未覆盖冻结契约。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:530-563`、`lib/js/src/311/mock-provider.ts:2280-2315`。
   - 问题：UI 只有 `event_type`、`actor_id`、`occurred_from`，缺少 `actor_type`、`entity_id`、`entity_type`、`request_id`、`source_channel`、`occurred_to`；Mock 的 `fixture-page-N` token 不绑定 filters/sort，旧 token 改筛选或排序仍可用。
   - 风险：审计查询能力不完整，分页可能漏/重记录，列表与导出无法形成契约一致的观察面。
   - 修复：补齐筛选控件/序列化/持久化并复用到导出；绑定 filters、sort、权限上下文，变化时返回 `INVALID_PAGE_TOKEN`，补过滤分页和空结果测试。

6. **CRM 数据导出 429 的 `Retry-After` 不符合冻结 fixture。**
   - 位置：`lib/js/src/311/mock-provider.ts:572-579`；契约 `server/compose/types/city311/contract.json:8872-8882`。
   - 问题：`rate-limited` 固定返回 `Retry-After: 30`，冻结 `data_export` fixture 要求 `Retry-After: 60`。
   - 风险：重试等待和前端提示与真实 API 不一致。
   - 修复：使用契约 fixture 的整秒值 `60`，并断言 429、`RATE_LIMITED`、`retryable` 和 header。

### 🟡 非阻断风险

- `lib/js/src/311/provider.ts:667,678` 的真实 HTTP adapter 不主动注入 workflow action/data export 所需的 `Authorization`；FE-11 真实联调前需完成 transport/header parity，不在本阶段写入秘密。
- `getOperation()` 对未知 operation id 返回成功空 operation（`mock-provider.ts:1134-1144`），建议后续按契约返回 `NOT_FOUND`。
- FE-09 浏览器矩阵已覆盖本阶段 1440/390；完整 360、767、768、1023、1024、1920 和 Edge/Chrome/Firefox/Safari 属于 FE-10。

### 结论

**Request Changes：FE-09 尚未完成，不能提交或合入。**

当前测试和浏览器 Mock 流程全部通过，但上述 6 项可直接复现的契约/权限阻断仍存在。请只修改前端 Mock/provider/types/fixtures/UI/test，关闭阻断后重新运行全部门禁；无需等待或修改后端。修复完成并提交前，确认 PR 仅包含 FE-09 变更、`git diff --check` 通过、工作区状态和测试/截图/日志证据齐全。

---

## 2026-09-08 18:36

## FE-09 阻断修复后复审（Frontend Mock-only）

### 审查目标

- 类型：本地 `FE-09` 分支工作区复审；HEAD `c6881d4`，与 `origin/2024.9.x` 一致。
- 范围：工作流、OAuth action、ICS、邮件、报告、审计、联系人邮件导出、CRM 数据导出及其 provider/mock/types/fixtures/UI/测试。
- 边界：所有前端实现按 Frontend Mock-only 审核；不要求真实 API、数据库或外部系统，不修改后端。Mock 仍必须遵守冻结契约的权限、分页、字段和错误语义。

### 自动化证据

- `corepack yarn --cwd lib/js test:unit`：`265 passing, 16 pending`。
- `corepack yarn --cwd lib/vue test:unit`：`53 passing`。
- `corepack yarn --cwd client/web/admin test:c311`：`94 passing`（仅既有 jsdom navigation warning）。
- `corepack yarn --cwd lib/js tsc --noEmit`、lib/js/lib/vue/Admin 构建：通过。
- FE-09 定向 ESLint（Mock/provider/types/fixtures/Extensions/routes）：通过；构建中的历史 lint、Sass、bundle-size warning 不是本轮新增错误。
- `C311_ARTIFACT_DIR=/tmp/c311-fe09-current tools/c311-browser/run-fe09.sh`：`44/44` 通过，覆盖 workflow designer、constituent、department manager、platform administrator，以及 `1440x900`、`390x844`；无页面错误、横向溢出或未预期请求。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_fe09_matrix.py'`：`2` 项通过。
- `git diff --check`：通过；工作区仍包含未提交的 FE-09 产品、测试和审查记录改动，未发现 `server/`、数据库或迁移修改。

### 已确认关闭

- ICS 同 UID 更新和 `STATUS:CANCELLED` 已通过再次导入实现，未知取消 UID 被计入 `ignored`，元数据和刷新恢复有测试。
- 工作流定义校验、测试执行日志、版本恢复和 OAuth 成功/`INVALID_CLIENT`/`INVALID_TOKEN`/`INSUFFICIENT_SCOPE`/`TEMPORARILY_UNAVAILABLE` Mock 场景已覆盖。
- 邮件模板、预览/发送校验、幂等 key、PENDING 到 DELIVERED/TERMINAL_FAILURE 查询闭环已覆盖。
- 审计范围/CSV 字段、联系人 verified email 与 opt-out 过滤、四类 CRM 导出实体投影已接入 Mock。

### 🔴 严重问题（仍阻断）

1. **CRM 导出 opaque page token 未绑定原查询。**
   - 位置：`lib/js/src/311/mock-provider.ts:2387,2465-2468`。
   - 证据：构造四条记录时，第一页 `page_size=1` 返回 `r2` 和 `opaque-next`；携带该 token 改用 `page_size=2` 后返回 `r4`、`request-fixture-001`，跳过 `r3`。改变 `filters`/`updated_since` 也会被接受。
   - 风险：分页结果不稳定，导出可能漏记录或重复记录，不能满足 FE-09 的导出分页要求。
   - 修复：token 必须保存并校验 entity、filters、updated_since、游标（以及实现选择的 page size）；上下文不匹配返回契约允许的 `INVALID_PAGE_TOKEN`，补充跨 page-size/filters 测试。

2. **报告导出未校验 `format=CSV`。**
   - 位置：`lib/js/src/311/mock-provider.ts:1926-1940`。
   - 证据：`exportReport('report-fixture-001', { format: 'JSON' as any })` 当前仍返回 `PENDING` operation；契约 `report_export` 的请求体要求 `format` 常量为 `CSV`。
   - 风险：Mock 接受真实 API 应拒绝的请求，前端会错误地把非 CSV 导出当作成功，造成 mock/real parity 偏差。
   - 修复：在创建 operation 前验证 format 且只接受 `CSV`，其他值返回 `VALIDATION_ERROR`/422；补充非法 format 无写入测试。

3. **保存报告的 resource owner 未建模，更新/共享可被非所有者执行。**
   - 位置：`lib/js/src/311/mock-provider.ts:1875-1887,2255-2277`、`lib/js/src/311/fixtures.ts:376-389`。
   - 证据：`service_agent`、`supervisor`、`workflow_designer` 均可对默认 `report-fixture-001` 调用 `updateReport`；`service_agent` 也可将其共享给自身角色。当前 fixture 没有报告所有者，范围比较使用当前 actor 而不是创建者。
   - 风险：违反契约 `saved_report_update`/`saved_report_share` 的 `resource_owner` 授权，可能越权修改或共享报告。
   - 修复：仅在 fixture metadata 中增加明确 owner；更新和共享要求 owner（平台管理员按契约授权），共享范围按创建者部门/地区重新计算；非法 body 返回 `VALIDATION_ERROR`，补充非所有者、同范围和扩大范围测试。

4. **OAuth 导航没有按 `workflow.execute` scope 隐藏。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:689`；路由守卫虽在 `client/web/admin/src/views/routes.js:134-137` 检查 scope。
   - 问题：`navItems()` 的 OAuth 项没有 `scope`，所以无该 scope 的员工仍看到 OAuth 链接，只能在点击后被重定向 403，违反 capability/scope 驱动操作显示。
   - 修复：为导航项声明 `scope: 'workflow.execute'`，并增加无 scope 角色导航不显示、直接 URL 仍返回 403 的组件/浏览器断言。

5. **审计过滤界面和 page token 仍未覆盖冻结契约。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:530-563`、`lib/js/src/311/mock-provider.ts:2280-2315`。
   - 问题：页面仅提供 `event_type`、`actor_id`、`occurred_from`，缺少契约定义的 `actor_type`、`entity_id`、`entity_type`、`request_id`、`source_channel`、`occurred_to`；Mock 的 `fixture-page-N` 不绑定 filters/sort，携带旧 token 改过滤条件仍返回结果。
   - 风险：FE-09 审计无法按完整条件查询，分页可能漏/重记录，且不能证明契约要求的 token binding。
   - 修复：补齐过滤控件、持久化和导出复用；token 绑定 filters/sort/权限上下文，变化时返回 `INVALID_PAGE_TOKEN`，补充过滤后分页和空结果保留过滤条件测试。

6. **CRM 导出 429 的 Retry-After 与冻结 fixture 不一致。**
   - 位置：`lib/js/src/311/mock-provider.ts:572-579`；契约 fixture `server/compose/types/city311/contract.json:8872-8882`。
   - 问题：`failIfNeeded('rate-limited')` 固定返回 `Retry-After: 30`，冻结 `data_export` 的 `rate_limited` response 要求 `Retry-After: 60`（且必须为整秒）。
   - 风险：Mock 错误头与真实 API/契约不一致，重试等待和前端提示会产生 parity 偏差。
   - 修复：使用契约 fixture 的 Retry-After 值，并断言状态码、错误码、retryable 和 header。

### 🟡 建议修改（不阻断本次 Mock 合入，但需登记）

- `lib/js/src/311/provider.ts:667,678` 的 HTTP adapter 不主动注入 workflow action/data export 所需的 `Authorization`；当前可由 transport/options 提供，FE-11 真实联调前必须补齐并做 mock/real parity，不要在本阶段写入秘密。
- `getOperation()` 对未知 operation id 返回成功空 operation（`mock-provider.ts:1134-1144`），建议按契约返回 `NOT_FOUND`，并补充错误测试。
- FE-09 浏览器矩阵当前为 1440/390 两个视口和 4 个角色；360、767、768、1023、1024、1920 及目标浏览器属于 FE-10，但提交记录应明确这一范围。
- 当前工作区 `Staff.vue` 包含大段格式化差异；提交时应确认只带 FE-09 所需产品/测试文件和审查记录，避免把无关格式化混入。

### 结论

**Request Changes：FE-09 仍未完成，不能提交或合入。**

本轮自动化门禁和主要 Mock 流程均通过，但以上 6 项契约/权限/分页阻断仍可复现；“已修复”不能以现有固定 fixture 的成功结果替代。请只在前端 Mock/provider/types/fixtures/UI/test 中关闭阻断，重新运行上述门禁并提交稳定条款编号、实现文件、fixture、测试/截图/日志、已知后端前置条件、`git diff --check` 和干净工作区证据。全程不修改后端或数据库。

---

## 2026-09-08 10:37

## FE-09 当前分支复审

### 审查目标

- 类型：本地工作区复审。
- 范围：FE-09 工作流、OAuth workflow action、ICS、邮件、报告、审计和导出前端页面，以及 C311 types/provider/mock/fixtures、组件测试和浏览器矩阵。
- 边界：Frontend Mock-only；不要求真实 API 联调，不修改或要求修改后端、数据库和迁移，但 Mock 必须遵循最新冻结契约。
- 基线：本地 `HEAD cf999f5`；最新 `origin/2024.9.x c6881d4`，当前分支落后 7 个提交。

### 自动化检查

- `corepack yarn --cwd lib/js test:unit`：256 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：53 passing。
- `corepack yarn --cwd client/web/admin test:c311 --runInBand`：94 passing。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_fe09_matrix.py'`：2 passing。
- `tools/c311-browser/run-fe09.sh`：18 个浏览器记录通过，覆盖 7 个路由、390/1440 两个视口。
- `Extensions.vue` lint：0 errors。
- Admin production build：通过；存在既有 Browserslist、Sass、bundle-size 和其他页面格式 warning。
- `git diff --check`：通过；未发现 server、数据库、迁移或后端实现差异。

### 严重问题

1. **OAuth 实现的业务对象错误。** `/c311/staff/oauth` 是公开 OIDC 登录/回调页面，调用 `startFederatedSignIn` 和 `completeFederatedSignIn`；FE-09 要求的是 workflow authenticated HTTP action 的 OAuth2 client-credentials 成功/失败结果，并在执行日志中展示。`executeWorkflowAction` 未被页面或浏览器流程调用，DTO 也缺少契约要求的 `request_id`、`execution_id`、`accepted_at`。

2. **ICS 更新/取消通过契约外方法完成。** `updateCalendarEvent` 和 `cancelCalendarEvent` 在 HTTP provider 中固定返回 501，浏览器矩阵却以 Mock 内存 mutation 及 `calendar_event_update/calendar_event_cancel` 写入计数作为通过条件。冻结契约只允许重复导入同 UID ICS 更新，以及导入 `STATUS:CANCELLED` 取消。当前 `importCalendar` 未实现同 UID 更新、已知 UID 取消、未知取消 UID 忽略与导入摘要。

3. **邮件重试通过契约外方法完成。** `retryMail` 在 HTTP provider 中固定返回 501，页面和 E2E 依靠 Mock-only `mail_retry` 推进 attempts/status。冻结契约只有 send/get delivery；邮件重试由服务端负责，前端应查询可观察的 delivery 状态，未知发送结果的重复操作应复用同一 Idempotency-Key。当前测试会为不可替换为真实 adapter 的行为提供假阳性。

4. **报告 DTO、分享请求和 CSV 结果不符合契约。** `ReportCatalogueItem` 使用 `report_id/name/entity`，契约要求 `report_key/name/supported_filters/supported_grouping/supported_sort`；`ReportShare` 使用 `audience/public`，契约要求 `roles`。页面 `makeCsvPreview` 在浏览器本地生成硬编码 CSV，E2E 验证的是这段伪造文本而不是 Mock export operation 的输出；最低五类报告目录也未证明。

5. **审计、联系人邮件和数据导出范围未完成。** 页面没有审计过滤控件，审计导出固定空过滤；数据导出固定 `service-requests`，没有实体、筛选、updated_since、分页和空结果过滤器连续性；没有 contact-email export 页面/provider；浏览器测试未覆盖 scope denial、无效 filter/page token、rate limit 和可重试/终止失败。

6. **异步操作缺少进行中到最终结果闭环。** workflow test、calendar import、report run/export、audit export 只显示初始 operation id，没有使用已有 `getOperation` 查询最终成功/失败、下载元数据或导入摘要，不满足 8.2.1 和 FE-09 退出条件。

### 亮点

- 工作流表单、报告边界控件、邮件模板/状态、页面路由和行为级浏览器矩阵已经形成基本界面骨架。
- 未发现前端包含 WORKFLOW、MAIL 或 OAuth 客户端秘密。
- 本次改动保持在前端 Mock 范围，没有触碰后端。

### 结论

- 结论：`Request Changes`。
- 严重问题：6 个。
- 当前不能提交或合入 FE-09。门禁通过只能证明现有 Mock 自洽，不能证明它符合冻结契约。
- 修复方向：删除契约外的 calendar mutation 和 mail retry provider 能力；用 ICS 重复导入、mail delivery 查询、workflow action、operation 查询和契约一致的 report/export fixtures 完成相同前端验收。补齐审计过滤、联系人邮件导出、数据导出范围及失败场景后，再更新到最新主线并重跑完整门禁。

---

## 2026-09-08 FE-09 修复后复审

### 结论

**Request Changes：当前 FE-09 仍未完成，不能提交合入。** 本轮 Mock 浏览器矩阵、单元/组件测试、生产构建和 `git diff --check` 均通过，但测试通过的是数个前端自定义的 Mock 行为，其中工作流 OAuth、ICS 更新/取消、邮件重试、报告目录/共享/导出与冻结契约不一致；审计和导出也未覆盖 FE-09 要求的功能范围。当前分支还落后最新 `origin/2024.9.x@c6881d4` 7 个提交。

### 阻断问题

1. **工作流 OAuth 实现成了公共 OIDC 登录。** 页面新增 `/c311/staff/oauth`，调用 `startFederatedSignIn`/`completeFederatedSignIn`；FE-09 要求的是工作流 HTTP 动作使用管理员配置的 OAuth2 client credentials，并在工作流执行日志显示响应状态或连接失败。现有 `executeWorkflowAction` 未被页面使用，且请求/响应 DTO 缺少契约要求的 `request_id`、`execution_id`、`accepted_at`。

2. **日历更新和取消使用了契约不存在的方法。** HTTP provider 的 `updateCalendarEvent`、`cancelCalendarEvent` 固定返回 501，只有 Mock 直接修改内存事件。冻结契约只提供 ICS import/export；同 UID 更新和 `STATUS:CANCELLED` 必须通过再次导入 ICS 实现。当前 `importCalendar` 只添加未知 UID，不会更新同 UID、取消已有事件、忽略并报告未知取消 UID，也没有导入摘要。

3. **邮件重试使用了契约不存在的 `retryMail`。** HTTP provider 固定返回 501，Mock 人工推进 attempts/status。契约只提供 send 和 delivery get；后台负责重试，前端应查询/刷新投递状态，并在不确定响应重放时复用同一幂等键。当前 UI 和 E2E 证明的是无法替换为真实 adapter 的 Mock-only 行为。

4. **报告 DTO 与冻结契约不匹配，并在前端伪造 CSV。** `ReportCatalogueItem` 使用 `report_id/entity`，契约要求 `report_key` 及 supported filters/grouping/sort；共享请求使用 `audience/public`，契约要求 `roles`。页面通过 `makeCsvPreview` 生成硬编码 CSV，而不是展示导出 operation 的真实 Mock 结果；目录也未提供要求的五个最低报告。

5. **审计和导出范围未完成。** 页面没有审计过滤器；审计导出始终发送空过滤条件；数据导出固定为 service requests，没有实体/筛选/分页/updated_since、无效筛选、无效 page token、rate limit 和 scope denial 的可观察处理；契约已有的 contact-email export 没有 provider/UI/E2E。

6. **关键异步操作没有完成状态闭环。** workflow test、calendar import、report run/export、audit export 只显示初始 `operation_id`，没有查询 operation、显示进行中/成功/失败或输出下载结果，不能满足 FE-09 对失败状态和导出结果可观察的退出条件。

### 自动化结果

- `lib/js` unit：256 passing / 16 pending。
- `lib/vue` unit：53 passing。
- Admin C311 components：94 passing。
- FE-09 Python 门禁：2 passing。
- `tools/c311-browser/run-fe09.sh`：18 个 Mock 场景/视口记录通过；但其中 OAuth、calendar mutation、mail retry、local CSV 正是上述非契约行为，因此不能作为契约完成证据。
- Admin production build：通过；存在既有 lint/deprecation/bundle-size warnings，无新增 build error。
- `Extensions.vue` lint：0 errors。
- `git diff --check`：通过。
- 本轮未修改或要求修改 server、数据库、迁移；FE-09 产品改动仍全部在未提交工作区。

### 修复边界

继续保持前端 Mock 合入。不要新增后端端点，也不要在 provider 中保留真实 adapter 永远返回 501 的伪能力。前端 Mock 必须模拟冻结契约自身：工作流 action、ICS 重复导入、mail delivery 查询、operation 查询、report/export 结果、audit/contact-email/data export 的成功与失败。

---

## 2026-09-07 FE-09 修复后复审

### 验证结果

- 当前分支：`FE-09`，产品修改仍未提交；工作区包含前端、Mock、测试和审查记录文件。
- `git diff --check`：通过。
- `corepack yarn --cwd lib/js test:unit`：253 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：90 passing。
- `Extensions.vue` lint：0 errors、222 warnings。
- FE-09 Python 门禁：2 passing。
- FE-09 浏览器矩阵：通过 4 角色、5 路由、2 视口的打开及点击 smoke 检查。

### 已改善

- 工作流页面增加了名称、触发器、conditions/actions JSON 编辑、保存、测试、激活/停用和执行日志入口。
- 报告页面增加了列数、分组数、排序数的基本 UI 校验，并增加共享按钮和报告目录请求。
- 邮件页面增加模板选择和 retry 按钮。
- 日历页面增加 UID 展示以及更新/取消按钮。
- 浏览器脚本开始执行这些按钮，但目前只检查页面最终无错误，没有断言操作结果。

### 🔴 仍然阻断的问题

1. **日历更新/取消没有进入 provider、Mock 或 API。** `Extensions.vue` 的 `updateCalendarEvent`、`cancelCalendarEvent` 仅修改本地 `calendarEvents` 数组；`provider.ts` 仍只有 `importCalendar`/`exportCalendar`，没有 UID update/cancel 方法。刷新页面或真实联调后状态会丢失，不能满足 UID 更新和取消契约。

2. **OAuth 成功/失败流程仍未实现。** 虽然 provider 有 `executeWorkflowAction`，但页面没有调用入口、OAuth 状态展示或 success/invalid-token/insufficient-scope/temporary-failure Mock 场景；`rg` 也未发现 FE-09 页面对 OAuth 结果的断言。因此工作流的 OAuth 要求仍未关闭。

3. **邮件 retry 不是重试生命周期。** `mail-retry` 复用相同 subject 生成的相同 `Idempotency-Key`，Mock 会直接返回之前的 `PENDING` 响应，既不会增加 attempts，也不会进入 `DELIVERED` 或 `TERMINAL_FAILURE`。页面没有终止失败操作，也没有对应状态迁移、写入计数和重试结果断言。

4. **报告仍未完成真实构建和导出验收。** 页面虽然显示计数并调用 share，但报告目录返回值被丢弃；没有过滤器控件，也没有验证 20/21 列、3/4 sort、分组边界、共享结果和 UTF-8 CSV 内容。Mock `createReport` 也没有对这些 schema 限制进行验证，非法定义可直接写入。

5. **行为级 E2E 仍是无断言点击。** `fe09_matrix.py` 点击保存、测试、激活、分享、邮件发送、日历更新/取消和导出后，只断言标题、溢出、console/HTTP 错误；没有从 DOM、provider 返回值、写入次数或 fixture 状态证明操作成功、失败恢复、capability 隐藏和幂等性。

### 结论

**Request Changes。FE-09 仍未完成，不符合直接合入条件。** 本轮是 UI 覆盖扩大，不是所有契约行为已经完成。请至少补齐日历真实 provider/Mock 操作、OAuth 成功/失败、邮件 retry/terminal 生命周期、报告 schema/CSV 验证，并让浏览器门禁断言结果后再提交复审。修复继续限定在前端 Mock，不修改后端或数据库。

---

## 2026-09-07 FE-09 分支审核

### 审查目标

- 分支：`FE-09`；当前 HEAD `cf999f5`，与 `origin/2024.9.x` 相同。
- 范围：当前工作区 FE-09 前端 Mock 改动，包括 `Extensions.vue`、C311 provider/mock/types/fixtures、路由和 FE-09 浏览器门禁。
- 边界：只审核前端 Mock；不要求、不修改 server、数据库或真实外部系统。
- 对照：FE-09 开发要求及主线已存在的 workflow、calendar、mail、report、audit、export 契约。

### 自动化证据

- `git diff --check`：通过。
- `corepack yarn --cwd lib/js test:unit`：253 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：90 passing。
- `Extensions.vue` lint：0 errors、172 warnings。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_fe09_matrix.py'`：2 passing。
- `tools/c311-browser/run-fe09.sh`：通过 4 个角色、5 条路由、1440/390 视口的页面打开/标题/溢出检查。

### 🔴 阻断问题

1. **工作流不是可编辑的创建/设计器流程，且缺 OAuth 成功/失败验收。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:14-23,61-63`。
   - 当前只有固定值“一键新建”、测试、激活/停用；没有触发器、条件、动作编辑，也没有版本编辑/保存和 `workflow_action_execute` 的 OAuth 成功/失败界面或测试。
   - 影响：不满足“工作流创建、触发器、条件、动作、测试、激活/停用、版本、执行日志及 OAuth 成功/失败状态”。
   - 修复：增加可提交的 workflow definition 表单，明确展示/编辑 trigger、conditions、actions、version；补充 OAuth action 的 success、invalid token、insufficient scope、temporarily unavailable Mock 场景及页面断言。

2. **日历只实现 ICS 导入/导出，缺 UID 更新和取消。**
   - 位置：`Extensions.vue:33-34,69-70`；`lib/js/src/311/provider.ts:303-304`。
   - 当前没有 UID 输入/展示、更新事件、取消事件的 provider 方法、Mock 状态或测试。
   - 影响：不满足“ICS 导入/导出、UID 更新和取消”；真实同步时容易产生重复事件或无法撤销事件。
   - 修复：按契约补充 UID、更新、取消的数据结构和 Mock 结果，验证同 UID 更新不新增事件、取消后状态为 `CANCELLED`，并增加浏览器断言。

3. **邮件缺少模板选择、失败生命周期和重试/终止操作。**
   - 位置：`Extensions.vue:30-31,67-68`；`lib/js/src/311/mock-provider.ts:1700-1735`。
   - 页面没有模板列表/`template_id` 控件；只显示首次 `PENDING` 结果，没有 retry、`TERMINAL_FAILURE` 终止态展示及相应恢复路径。Mock 也未提供可观察的重试/终止生命周期。
   - 影响：不满足邮件模板选择、编辑、预览、发送、可重试和终止失败要求，无法验收“失败后不重复写入、重试后状态变化”。
   - 修复：增加模板 fixture/选择控件，覆盖 preview/send；增加 retryable、terminal fixture 和页面操作/状态断言，确保重试使用新幂等语义且不会重复创建投递。

4. **报告功能不是要求中的报告目录/构建器，且未执行契约边界校验。**
   - 位置：`Extensions.vue:25-28,64-66`；`mock-provider.ts:1586-1600`。
   - 页面直接调用 `listReports()`，没有使用已定义的 `listReportCatalogue()`；没有过滤器、分组、排序控件、共享操作；列输入不限制最多 20 列，排序不限制最多 3 列，未验证一个分组和 UTF-8 CSV 结果。
   - 影响：无法证明 FE-09 的报告目录、构建器、最多 20 列/一个分组/最多三列排序、保存/共享和 CSV 导出要求，非法定义可能被 Mock 接受并进入后续真实 API 阶段。
   - 修复：使用 catalogue 作为能力入口，补齐 builder 字段和客户端校验；Mock 与组件测试覆盖 20/21 列、4 个 sort、多个 grouping、share、空结果和 UTF-8 CSV。

5. **FE-09 浏览器门禁只有壳层 smoke 检查。**
   - 位置：`tools/c311-browser/fe09_matrix.py:22-45`。
   - 当前只断言一个主标题、无横向溢出、无页面错误/异常响应；没有点击工作流、邮件、日历、报告、审计、导出操作，也没有检查角色 capability 隐藏、错误/重试、分页和持久化结果。
   - 影响：测试通过不能证明 FE-09 的关键交互完成，存在“路由可打开但功能不可用”的回归风险。
   - 修复：在至少一个稳定角色和视口执行每类成功/失败路径，并从 DOM、Mock 写入计数和返回状态断言结果；保留全角色/视口矩阵作为布局门禁。

### 🟡 建议修改

- `Extensions.vue` 目前 172 条 lint warning，虽然不是 CI 阻断，但建议在提交前按项目 Vue 格式拆分模板；过长单行模板会降低后续修复可读性。
- 当前 FE-09 修改仍在工作区未提交，且 `docs/代码审查.md`、`docs/c311/` 与产品文件混在一起；提交时应只包含 FE-09 产品/测试文件。

### 结论

**Request Changes。当前 FE-09 未完成，不符合直接合入条件。**

provider 的基础 endpoint 映射、角色能力 fixture 和页面壳层已具备，自动化门禁也通过；但工作流设计、OAuth、ICS UID 更新/取消、邮件生命周期、报告构建器/边界校验以及行为级 E2E 均未完成。请先关闭 5 个阻断项，再提交仅包含 FE-09 的变更并重新运行完整门禁。修复范围保持前端 Mock，不要修改后端或数据库。

---

## 2026-09-07 FE-08 远端主线与契约 PR 核验

### 审查输入

- 远端主线：`origin/2024.9.x@47573f1`，截至 2026-09-07 尚未包含 PR #50/#51。
- PR #50：帮助生命周期契约，状态为 OPEN，目标分支 `city311/dev3-branding-content`；检查通过且 GitHub 标记为可合并。
- PR #51：类别并发契约，状态为 OPEN，目标分支 `city311/dev3-categories-custom-fields`；检查通过且 GitHub 标记为可合并。
- 依赖链仍未进入主线：#33（branding/content）和 #34（categories/custom-fields）均为 OPEN；#50/#51 不是直接针对 `2024.9.x` 的 PR。
- 当前本地 `FE-08` 仍基于旧主线，且 FE-08 实现/测试均未提交；没有可供远端审查的 FE-08 PR。

### 契约核验

- 当前主线仍只有 `admin_help_update`，没有帮助 preview/publish/versions/rollback 的 operation、path、capability 和 schema。
- 当前主线的 `admin_categories_update` 没有声明 `If-Match`、409 `VERSION_CONFLICT` 或 428 `EXPECTED_VERSION_REQUIRED`。
- PR #50 的契约已补充帮助生命周期接口及强 ETag/If-Match 规则；PR #51 已补充类别更新并发协议。故此前阻断不是实现不存在，而是契约尚未合入主线。

### 本地 FE-08 门禁

- `corepack yarn --cwd lib/js test:unit`：253 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：90 passing。
- `Config.vue` lint：0 errors、254 warnings。
- FE-08 Python 门禁：2 passing；FE-08 浏览器矩阵：通过。
- `git diff --check`：通过；本轮工作区没有 server/数据库修改。

### 结论

**Conditional Approval / 暂缓合入：不能直接合入当前 `2024.9.x` 主线。** FE-08 前端 Mock 实现和本地测试已达到实现层面要求，但其新增调用依赖尚未进入主线的 #50/#51 契约。先按依赖顺序合入 #33 → #34 → #50/#51（或形成包含同等契约的主线 PR），再将 FE-08 rebase 到包含这些契约的最新主线、重新运行 CI 和 FE-08 门禁；远端 FE-08 PR 基于该结果且 CI 全绿后，才可批准合入。不得为了提前合入而修改或伪造主线契约，也不要把后端改动混入 FE-08。

---

## 2026-09-07 FE-08 再次复审

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，253 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：通过，90 tests。
- `vue-cli-service lint --no-fix src/views/C311/Config.vue`：无 error，240 warnings。
- `tools/c311-browser/run-fe08.sh`：通过，1440 与 390 CSS 像素 Mock 页面检查通过。
- 本轮仍未发现后端、数据库或 `server/` 文件变更；按前端 Mock 独立合入规则审核。

### 已关闭问题

- 公共品牌读取不再要求管理员 capability。
- 自定义字段已增加默认值编辑与 Mock 校验。
- 类别使用中停用保护已加入 Mock。
- Config 页面 lint error 已清零；剩余为格式 warning，不阻断 CI。

### 🔴 仍未完成

**帮助管理生命周期仍不符合 FE-08**

- 位置：`client/web/admin/src/views/C311/Config.vue:46-53`、`lib/js/src/311/provider.ts:248`
- 当前只有 `updateAdminHelp`/保存按钮，页面明确标注帮助预览、发布、版本历史和回滚未暴露。
- FE-08 要求帮助内容支持草稿、预览、发布、版本和回滚；这是前端 Mock 契约缺失，不是后端依赖问题。
- 必须补齐 Mock provider、HTTP adapter、权限 capability、版本冲突和页面交互测试。

**类别更新仍不支持 If-Match 并发控制**

- 位置：`lib/js/src/311/provider.ts:250-251`、`client/web/admin/src/views/C311/Config.vue:56-68`
- `updateAdminCategory` 没有 `C311RequestOptions`，Mock 也不会验证 expected version；页面仍提示该限制。
- FE-08 的管理资源版本并发要求不能仅因当前“冻结契约”缺少参数而跳过；应在前端 Mock/provider 契约中补齐并测试 409 冲突恢复。

### 🟡 质量缺口

- FE-08 浏览器矩阵目前只覆盖 390 和 1440 CSS 像素；完整 360、767、768、1023、1024、1920 视口矩阵仍应由 FE-10 补齐，但 FE-08 页面至少应增加关键管理操作的移动端证据。
- 仍有 240 条 lint warning，虽不阻断 CI，但建议在 FE-10 统一格式化。

### 结论

- 结论：`Request Changes`
- 严重问题：2 个
- 质量缺口：2 个
- 当前不能合入 FE-08。前端 Mock 需要先补齐帮助的预览/发布/版本/回滚生命周期，并为类别更新增加 If-Match、409 冲突和输入保留测试；无需等待或修改后端。

---

## 2026-09-07 FE-08 复审

### 审查目标

- 类型：当前本地 FE-08 工作区改动复审
- 范围：管理端 Config 页面、C311 provider/types/mock/fixtures、路由和 FE-08 浏览器矩阵
- 约束：前端 Mock 合入，不修改后端、数据库或 server 业务逻辑

### 验证证据

- `corepack yarn --cwd lib/js test:unit`：通过，252 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：通过，90 tests。
- `tools/c311-browser/run-fe08.sh`：通过，1440 与 390 CSS 像素页面矩阵通过；仅允许开发壳的 `custom.css`/`code-snippets.js` 500 噪声。
- `vue-cli-service lint --no-fix src/views/C311/Config.vue`：失败，3 个 error、237 个 warning；至少存在 3 个 `object-property-newline` 错误。
- 本轮差异未发现 `server/`、Go 或数据库文件。

### 已关闭的问题

- 公共 `getBranding()` 已恢复为公共读取，不再要求管理员 capability；Mock 回归测试覆盖匿名和居民读取。
- 品牌/内容已有版本列表、回滚入口和内容预览的安全文本展示。
- 管理页面已有基础帮助 key/语言编辑入口、自定义字段编辑/停用/验证/选择值顺序入口。

### 🔴 仍需修复

**问题1：帮助管理仍未完成 FE-08 契约**

- 位置：`client/web/admin/src/views/C311/Config.vue:46-53`
- 页面明确显示“Help preview, publish, versions and rollback are not exposed by the frozen contract”，且 provider 只有 `updateAdminHelp`，没有帮助预览、发布、版本历史和回滚接口/交互。
- FE-08 要求帮助内容支持草稿、预览、发布、版本、回滚及稳定 key 本地化；当前只能保存帮助文本，不能完成验收。

**问题2：类别更新仍没有乐观并发保护和使用中保护**

- 位置：`lib/js/src/311/provider.ts:424-426`、`client/web/admin/src/views/C311/Config.vue:56-68`
- `updateAdminCategory` 没有 `C311RequestOptions`/`If-Match`，Mock 也不会拒绝过期版本；页面还将该缺口作为 contract limit 展示。
- FE-08 的管理资源并发要求及联系人类别“使用中保持活动”的规则无法验证，可能发生并发覆盖或非法停用。

**问题3：自定义字段缺少默认值及跨表面一致性验证**

- 位置：`client/web/admin/src/views/C311/Config.vue:70-82`、`lib/js/src/311/311.test.ts`
- 字段编辑表单没有 `default` 输入；测试只验证 provider CRUD，没有证明活动字段同步出现在请求表单、搜索、详情、报告和导出入口。
- FE-08 退出条件要求字段定义在各表面一致，当前证据不足，且默认值能力缺失。

**问题4：前端 lint 失败，不能作为可合入提交**

- 位置：`client/web/admin/src/views/C311/Config.vue:95,98,100`
- 当前文件存在 3 个 ESLint error。CI 的 Admin lint 会因此失败，即使单元测试和浏览器矩阵通过也不能判定可合入。

### 🟡 建议修改

- 路由 `c311.admin.config` 当前只声明 `requiresAuth`，建议补充管理 capability 集合，使路由守卫和页面 capability 过滤保持一致。
- 补充 Config 组件测试，覆盖版本冲突后输入保留、帮助多语言切换、回滚确认、类别停用保护和自定义字段默认值。

### 结论

- 结论：`Request Changes`
- 严重问题：4 个
- 建议修改：2 个
- 当前不能合入 FE-08。修复帮助完整生命周期、类别并发/使用中保护、自定义字段默认值与跨表面证据，并清除 lint error 后，再重新审核；仍无需等待或修改后端实现。

---

## 2026-09-06 FE-08 审查

### 审查目标

- 类型：当前本地 FE-08 工作区改动（前端 Mock 合入范围）
- 范围：`client/web/admin/src/views/C311/Config.vue`、C311 路由、`lib/js/src/311` provider/types/fixtures/mock，以及 FE-08 浏览器矩阵
- 约束：不审后端实现，不要求 FE-09 及后续功能

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，252 passing / 16 pending。
- `corepack yarn --cwd client/web/admin test:c311`：通过，90 tests。
- FE-08 浏览器脚本仅静态检查 1440/390 两个视口和四个配置标签，未覆盖发布、回滚、冲突、权限操作。

### 问题清单

#### 🔴 严重问题

**问题1：公共品牌读取被错误地绑定管理员 capability**

- 位置：`lib/js/src/311/mock-provider.ts:803-807`
- 问题：`getBranding()` 原本对应公共 `/api/v1/public/branding`，现在调用 `requireCapability('admin_branding_get')`。
- 风险：匿名门户和普通已登录居民没有该 capability，Mock 模式加载公共门户时会变成 401/403，破坏 FE-02/FE-05 已有流程。
- 建议：恢复 `getBranding()` 的公共读取语义；管理员读取保持在 `getAdminBranding()`，并增加匿名/居民回归测试。

**问题2：FE-08 要求的版本历史和回滚没有页面入口**

- 位置：`client/web/admin/src/views/C311/Config.vue:12-19`
- 问题：虽在 provider 中声明了 `list*Versions`、`rollback*`，Config 页面没有历史列表、目标版本选择、回滚确认或冲突恢复 UI；内容和品牌均如此。
- 风险：品牌/内容的“版本历史和回滚”验收无法完成，声明的契约能力实际不可用。
- 建议：为品牌和每个内容 key 增加历史查看、回滚、If-Match 冲突提示并保留编辑输入的完整流程。

**问题3：帮助管理功能未实现**

- 位置：`client/web/admin/src/views/C311/Config.vue:17,42-48`
- 问题：页面只有公共内容正文编辑，没有稳定 help key、语言选择、帮助正文编辑、预览/发布/回滚或 `updateAdminHelp` 调用。
- 风险：FE-08 的“帮助草稿/预览/发布/回滚、本地化”条款缺失。
- 建议：增加 help key/语言编辑界面及对应版本化保存、预览、发布和回滚状态。

**问题4：预览直接使用 `v-html` 渲染未证明已清理的输入**

- 位置：`client/web/admin/src/views/C311/Config.vue:17`
- 问题：`previewAdminContent()` 的 Mock 返回值直接合并用户输入，随后通过 `v-html` 渲染；本地未见清理或安全 fixture 验证。
- 风险：一旦 provider/后端返回未清理 HTML，管理预览可触发 XSS；与“HTML 只显示后端清理后的内容”约束不相符。
- 建议：在 adapter/mock 中明确只返回清理后的 HTML，或前端预览使用已验证的清理函数，并增加恶意标签不会执行的测试。

**问题5：类别和自定义字段管理未满足完整配置能力**

- 位置：`client/web/admin/src/views/C311/Config.vue:18-19`、`lib/js/src/311/provider.ts:424-429`
- 问题：类别更新没有 `If-Match`/expected version；自定义字段页面只能创建和查看，不能编辑、停用、配置验证规则或维护选择值顺序。
- 风险：并发覆盖和配置不一致；FE-08 要求的自定义字段定义、验证、选择值顺序、停用无法验收。
- 建议：给类别更新补充版本参数和冲突恢复；补齐字段编辑/停用、required、validation、choice_values 顺序和版本化保存。

### 结论

- 结论：`Request Changes`
- 严重问题：5 个
- 建议修改：0 个
- 可选优化：0 个
- 当前不能合入 FE-08；应先修复上述问题并补充覆盖管理操作、权限矩阵、版本冲突、发布/回滚和 HTML 清理的 Mock/E2E 证据。所有改动仍应限定在前端及 C311 Mock/provider 契约层，不触碰后端。

---

## 2026-09-04 13:58 CST — FE-07

### 审查范围

- 分支：`FE-07`
- 基线：`origin/2024.9.x` / `2da61e02979b44e4d4802641fa7532441aecb7e6`
- 审查提交：`b784d733b9abf75c0bae4e13adfb3b448f1bb41e`
- 边界：仅审核前端 Mock、fixture、provider、管理端页面及其测试；不要求或修改后端实现。
- 对照：改造技术方案、前后端拆分方案、前端开发计划中的 FE-07 与条款 7.8.2–7.8.4、7.9.1、7.9.5、10.1–10.4、10.7、11.1、12.2.3。

### 结论

**Request Changes。当前分支尚未完成 FE-07，不应以 FE-07 完成态提交或合入。**

### 阻塞项

1. **批量操作未保证全成全败。** `mock-provider.ts:1360-1371` 逐条原地写入，没有暂存或回滚。使用现有 `pagination` fixture 批量关闭两条已解决请求时，第二条返回 `NOT_FOUND`，第一条已经变为 `CLOSED`，直接违反“批量失败时所有记录保持不变”。同时未校验所有记录属于同一部门及同一 `duplicate_group_id`。
2. **员工主流程无法完整执行。** `Staff.vue:208` 未把 `ASSIGN` 纳入可打开的状态转换，导致请求完成分诊后无法从 `TRIAGED` 进入 `ASSIGNED`。`Staff.vue:132` 的分诊会立即写入，没有让操作员确认服务类型、负责部门、来源分类、位置要求和重复组。
3. **协作者与提醒生命周期的页面能力不完整。** `Staff.vue:136` 的“管理协作者”只移动焦点，没有新增/移除控件；`Staff.vue:142` 只允许完成第一条提醒，没有逐条选择、延后/暂停或取消，也没有输入新的 `due_at`。Mock 的延后操作覆盖 `due_at`，没有保留旧到期时间的历史。
4. **409 冲突恢复与批量确认不满足退出条件。** `Staff.vue:154` 不显示 `current_version`，且无需重新加载即可用旧版本直接“重新应用”；批量错误完全绕过该恢复流程。批量确认框 `Staff.vue:49-50` 不显示选中记录数和具体变更，失败后也不展示 `failing_request_id` 或提供重新加载/重新应用路径。
5. **CivicWorks 映射和角色 fixture 与契约仍有偏差。** `mock-provider.ts:1399-1402` 对跨多步状态的完成事件不会完成 CRM 合法生命周期归一化；`fixtures.ts:238` 给 `platform_administrator` 暴露了 `staff_request_bulk`，而冻结契约只允许 `supervisor` 与 `department_manager`。
6. **现有测试门禁存在假阳性。** `fe07_matrix.py:63-70` 只打开并取消批量确认，不提交批量操作；`fe07_matrix.py:73-121` 未验证分派、协作者增删、提醒完整生命周期、版本冲突恢复和 CivicWorks 状态闭环；`test_fe07_matrix.py:14-25` 主要检查源码字符串。当前所有测试虽通过，仍未捕获第 1 项可复现的数据部分写入。

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，240 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：通过，53 passing。
- `corepack yarn --cwd client/web/admin test:c311`：通过，82 tests。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_*.py'`：通过，22 tests。
- `tools/c311-browser/run-fe07.sh`：退出码 0，角色 × 7 个视口矩阵通过；但矩阵未覆盖上述关键操作闭环，并允许了 mock 容器的已知 GET 500。
- `git diff --check 2da61e0..b784d733`：通过。
- Codex Security 分支差异扫描：14 个源代码表面完整覆盖，未发现可报告的软件安全漏洞；功能正确性阻塞项不因此解除。

### 合入前最低修复与回归条件

- Mock 批量操作先完成整批校验，再一次性提交；任何失败都不改变任一请求，并返回失败请求标识和当前版本。
- 页面可完整执行分诊确认、首次分派、重新分派、协作者新增/移除、状态机、提醒创建/延后/完成/取消。
- 409 页面显示当前版本、保留用户输入，并强制走“重新加载后重新应用”；批量冲突具备同等恢复能力。
- CivicWorks fixture 覆盖重复事件、低版本事件、非法签名、合法跨状态完成归一化，且同一事件只产生一次状态/历史/审计更新。
- 补充真实行为断言的组件与 Mock E2E，至少包含状态机、批量全成全败、提醒完整生命周期、外部事件幂等和单条/批量版本冲突。
- 修复后重新运行上述全部测试与 `git diff --check`，工作区不得引入无关文件。

---

## 2026-09-04 15:18 CST — FE-07 修复复审

### 审查范围

- 分支：`FE-07`
- 已提交 HEAD：`b784d733b9abf75c0bae4e13adfb3b448f1bb41e`
- 本轮对象：HEAD 之后 10 个 FE-07 前端 Mock、fixture、provider、页面和测试工作区修改。
- 边界：仅审核前端 Mock；不要求、审核或修改 server 业务逻辑和数据库。
- 排除：用户已有的 `docs/代码审查.md` 修改以及本审查记录目录。

### 结论

**Request Changes。当前工作区关闭了上次的大部分问题，但仍未完成 FE-07，暂不可作为 FE-07 完成态提交或合入。**

此外，本轮 10 个 FE-07 修复文件尚未提交；即使以下功能问题修复完成，也需要先形成只包含相关文件的提交再做最终合入审核。

### 已确认修复

- 批量执行已加入整批预校验、同部门/同重复组检查以及异常回滚。
- 页面已补充分诊确认、首次分派状态转换、协作者增删、逐条提醒操作、批量确认摘要和冲突版本展示。
- CivicWorks Mock 已补充合法跨状态归一化、事件幂等和状态回滚。
- `staff_request_bulk` fixture 已收敛为 supervisor 与 department_manager。
- 单元、组件、Python 门禁和真实浏览器矩阵均已扩充并通过。

### 阻塞项

1. **[P1] 批量 `CLOSE` 可以成功返回但最终状态不是 `CLOSED`。** `Staff.vue:43-46` 允许同时选择 `CLOSE` 和任意状态；`mock-provider.ts:1399-1416` 先按原状态校验并写入 `CLOSED`，随后又写入 `changes.status`。针对性复现以一个 `RESOLVED` 请求执行 `CLOSE + status=REOPENED`，结果返回 `updated_count=1`，但最终状态为 `REOPENED`、版本从 1 增到 3、历史为 `SUBMITTED → CLOSED → REOPENED`。这违反批量关闭的明确意图，也会制造误导性成功结果。应在 UI 和 provider 中把 `CLOSE` 与 `changes.status` 设为互斥，或明确拒绝该组合，并补充最终状态、版本和历史断言。
2. **[P1] 批量员工备注并未创建员工备注，且批量优先级缺少页面入口。** `mock-provider.ts:1416` 把 `staff_note` 拼成 `BULK_NOTE:*` 审计动作，没有写入请求的 `notes` 投影；复现后 `notes=[]`。`types.ts:474-478` 与 FE-07 契约包含 `priority`，但 `Staff.vue:43-46,193` 没有优先级字段。应将 `staff_note` 写入与员工详情一致的 staff-note 数据模型，并在页面、确认摘要、Mock 和测试中完整覆盖 `priority`。
3. **[P1] 重新分派只在错误的状态/动作组合下可见，并丢失必需审计内容。** `Staff.vue:141` 要求 `available_actions` 包含 `ASSIGN`，而 `mock-provider.ts:579-588` 在 `ASSIGNED` 和 `IN_PROGRESS` 仅提供 `START_PROGRESS`/`RESOLVE`，因此请求真正进入已分派或处理中后，重新分派入口消失。初始 SUBMITTED fixture 又错误地带有 `ASSIGN`（`fixtures.ts:77,85`），让现有浏览器测试在分诊前执行“重新分派”，掩盖了问题。并且 `mock-provider.ts:1229-1238` 校验 reason 后将其丢弃，审计只有 `ASSIGN`，没有旧受让人、新受让人和原因。应把首次分派状态转换与重新分派操作分开建模，并验证 ASSIGNED/IN_PROGRESS 重新分派后的状态、协作者保持、版本和完整审计。
4. **[P1] Mock 的 409“重新加载并重新应用”只能显示按钮，不能完成恢复。** `failIfNeeded()`（`mock-provider.ts:331-339`）对 `version-conflict` 与 `bulk-version-conflict` 每次变更都固定抛 409；现有组件测试 `c311-components.spec.js:783-825` 和浏览器矩阵 `fe07_matrix.py:203-226` 只验证重新加载后按钮出现，没有点击重新应用并验证成功结果。因此 Mock-only 验收要求的恢复闭环尚不存在。应让冲突场景只在第一次旧版本提交时失败，重新加载后以当前版本重试成功，并断言保留的输入、最终状态/受让人、版本、批量结果及无重复写入。
5. **[P2] 提醒 Mock 接受冻结契约之外的渠道和非法时间。** 契约枚举及 `enums.ts:38` 只允许 `EMAIL`、`IN_APP`，但 `mock-provider.ts:1267` 额外允许 `SMS`，且没有校验 `due_at` 可解析。使用 service_agent 创建 `channel=SMS, due_at=not-a-date` 的提醒会被接受。应复用冻结枚举并对无效/非未来时间返回 422，增加 provider 和浏览器负例。
6. **[P2] 批量确认的记录数可能与实际提交记录不一致。** `applyFilters()` 和翻页不会清除/裁剪 `selectedRequestIds`（`Staff.vue:240,255`），确认框显示全部已选数量（`Staff.vue:51`），提交时却只从当前页 `items` 过滤构造请求（`Staff.vue:290`）。筛选或翻页后可能显示 N 条、实际发送更少甚至空数组。应在列表变化时将选择集与当前项同步，或保存完整选择快照，并让确认数量和发送 ID 使用同一不可变集合。
7. **[P2] 分诊原因输入被忽略。** 分诊表单绑定 `triageForm.reason`（`Staff.vue:149`），但 `submitTriage()` 转到 `submitTransition()` 后读取的是 `transitionForm.reason`（`Staff.vue:265-271`）。应把分诊原因显式传给 transition Mock，并断言审计/请求中收到的原因。

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，244 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：通过，53 passing。
- `corepack yarn --cwd client/web/admin test:c311`：通过，87 tests。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_*.py'`：通过，22 tests。
- `tools/c311-browser/run-fe07.sh`：通过，退出码 0；角色/视口 Mock 矩阵没有网络写入。已知 `custom.css`、`code-snippets.js` GET 500 被门禁标记为允许项。
- 针对性 Mock 复现：批量关闭返回成功但最终 `REOPENED`、版本 3、备注数组为空；service_agent 的 `SMS + not-a-date` 提醒被接受。
- `git diff --check`：通过。
- Codex Security 工作区差异扫描 `2a332a6c-91a0-4797-a456-415929372035`：10/10 文件完整覆盖，0 个可报告漏洞；扫描仅证明本次前端 Mock 差异中未发现安全漏洞，不解除上述功能正确性问题。

### 再次复审的退出条件

- 为上述 7 项逐项添加失败先行的 provider/组件/浏览器测试，并确认修复后通过。
- 批量 `CLOSE` 最终必须为 `CLOSED` 且一次操作只产生约定的版本/历史；staff note、priority、确认集合均与页面结果一致。
- ASSIGNED 与 IN_PROGRESS 请求可以重新分派，原因和旧/新受让人可在 Mock 审计中验证。
- 单条与批量 409 流程都必须实际点击“重新应用”并成功，而非只验证按钮出现。
- 提醒仅接受 `EMAIL`/`IN_APP` 和合法未来时间。
- 提交前仅纳入 FE-07 相关文件，重新运行全部门禁和 `git diff --check`，并确认工作区没有无关内容。

---

## 2026-09-04 16:58 CST — FE-07 第二次修复复审

### 审查范围

- 分支：`FE-07`；已提交 HEAD：`b784d733b9abf75c0bae4e13adfb3b448f1bb41e`。
- 本轮对象：HEAD 之后 10 个 FE-07 前端 Mock、fixture、provider、页面和测试工作区修改。
- 边界：只审核前端 Mock 合入；未修改、未要求实现 server 业务逻辑或数据库。
- 对照：改造技术方案、前后端拆分方案、前端开发计划、`docs/前端开发计划指令.md:105-111`，以及条款 7.8.2～7.8.4、7.9.1、7.9.5、10.1～10.4、10.7、11.1、12.2.3。

### 结论

**Request Changes。上一轮 7 项阻塞均已关闭，但完整复核仍发现 4 项 FE-07 前端 Mock 缺口；当前不能认定 FE-07 开发完成，也不能作为完成态合入。**

此外，本轮修复仍只存在于本地工作区，HEAD 未包含这些修复。即使功能缺口关闭，也必须形成仅包含 FE-07 相关文件的提交后再进行最终合入审核。

### 已确认关闭的上一轮问题

- 批量 `CLOSE` 与第二次状态变更已互斥；批量字段、员工备注、优先级、同部门/同重复组预校验和失败回滚已补齐。
- ASSIGNED/IN_PROGRESS 重新分派入口、原因与旧/新受让人审计字段已补齐。
- 单条与批量 409 已能在重新加载后实际重新应用，且保留输入。
- 提醒渠道/时间的 provider 校验、提醒暂停历史、选择集同步、分诊原因传递均已修复。
- 角色 × 7 个视口浏览器矩阵和相关单元/组件门禁已扩充并通过。

### 仍阻塞 FE-07 的问题

1. **[P1] 重新分派仍未模拟或验证前后受让人通知。** `lib/js/src/311/mock-provider.ts:1260-1264` 只更新主受让人、版本和审计，没有产生原受让人与新受让人的通知结果；现有单元与浏览器断言也只检查状态、受让人和审计。该行为直接缺少 `docs/前端开发计划指令.md:109` 与 10.2.6 要求的通知副作用。修复应只落在前端 Mock/fixture/test，不需要触碰后端。
2. **[P1] CivicWorks 详情投影与浏览器可见结果不符合冻结契约。** `lib/js/src/311/types.ts:545-550` 的 `CivicWorksWorkOrder` 缺少必需的 `service_request_number`、`external_status_url`、`created_at`；`StaffServiceRequestDetail.external_work_order` 又退化为 `Record<string, unknown>`。`mock-provider.ts:1505` 写出的对象同样缺字段，`Staff.vue:134` 只把不完整对象序列化为 JSON。针对性运行还确认 TRIAGED→ASSIGNED 后 `external_work_order` 仍为 `null`。前端不应直连 CivicWorks，但 Mock 的详情/转换响应必须提供契约完整工单，并在界面明确显示工单号、状态和 URL。
3. **[P1] CivicWorks Mock 会接受契约非法事件，且缺少规定的旧版本/E2E 幂等证据。** `mock-provider.ts:1470-1471` 未校验 `previous_status`、`status` 枚举及 `occurred_at`；运行时以 `status=BOGUS, occurred_at=not-a-date` 调用仍返回 acknowledged，写入非法 external work order 并计一次回调写入。`311.test.ts:1247-1252` 只覆盖成功、重复和无效签名，`fe07_matrix.py:200-210` 也只执行一次成功回调，没有观察旧版本事件、重复/重试后单一 CRM 转换。应复用 `CIVICWORKS_STATUSES` 和 ISO 时间校验，并补齐旧版本、非法 payload、重复/重试以及一次转换/历史/审计的 provider 与浏览器断言。
4. **[P2] 提醒创建界面无法选择通知渠道。** `Staff.vue:157` 只有标题、到期时间和收件人字段，`Staff.vue:198` 将渠道隐藏固定为 `IN_APP`。因此无法从页面创建 `EMAIL` 提醒，也无法验收条款 7.8.4/10.4.3 的两种渠道。应增加只包含 `EMAIL`、`IN_APP` 的必填选择控件，展示已创建提醒的收件人与渠道，并对两种渠道增加组件及浏览器用例。

### 测试证据

- `corepack yarn --cwd lib/js test:unit`：通过，249 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：通过，53 passing。
- `corepack yarn --cwd client/web/admin test:c311`：通过，89 tests。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_*.py'`：通过，22 tests。
- `tools/c311-browser/run-fe07.sh`：通过，全部角色 × 360/767/768/1023/1024/1440/1920 CSS px；无意外页面错误和网络写入。开发壳已有的 `custom.css`、`code-snippets.js` 500 仍为允许噪声。
- 针对性 Mock 复现：分派结果为 `status=ASSIGNED, primary_assignee_id=null, external_work_order=null`；非法 CivicWorks 事件被接受并写入 `{status: BOGUS, updated_at: not-a-date}`。
- `git diff --check`：通过。
- Codex Security 扫描 `a3e95c45-12cc-4f88-98d2-73e4e0243521`：10/10 变更文件完整覆盖，0 个可报告安全漏洞；TAC 工具不可用，且真实后端边界按用户要求排除。

### 再次复审退出条件

- 只修改前端 Mock/fixture/type/UI/test，关闭上述 4 项，不新增 server 或数据库变更。
- 重新分派的原/新受让人通知在 Mock 状态或可观察日志中各一次，并验证重试不重复通知。
- CivicWorks 工单投影与冻结 schema 一致；非法事件不改变任何请求/外部工单；旧版本与重复/重试事件只产生一次合法 CRM 转换。
- 页面可选择并展示 EMAIL/IN_APP 提醒渠道。
- 浏览器门禁补充多记录批量失败后全部不变、CivicWorks 旧版本/重复回调以及两种提醒渠道；全部既有门禁和 `git diff --check` 通过。
- 形成只包含 FE-07 相关文件的提交并确认工作区没有混入 `docs/代码审查.md` 等用户原有改动。

---

## 2026-09-04 17:56 CST — FE-07 第三次修复复审

### 审查范围

- 分支：`FE-07`；已提交 HEAD：`b784d733b9abf75c0bae4e13adfb3b448f1bb41e`；基线：`origin/2024.9.x` / `2da61e02979b44e4d4802641fa7532441aecb7e6`。
- 本轮对象：HEAD 之后 10 个 FE-07 前端 Mock、fixture、provider、UI 与测试工作区修改，并回看完整 FE-07 差异。
- 边界：全部前端开发按 Mock 独立合入审核；不要求、不修改、不等待 server 业务逻辑、数据库或真实 CivicWorks。
- 对照：改造技术方案、前后端拆分方案、前端开发计划、`docs/前端开发计划指令.md:105-111`，以及条款 7.8.2～7.8.4、7.9.1、7.9.5、10.1～10.4、10.7、11.1、12.2.3。

### 结论

**Request Changes。上次 4 个实现缺口的核心代码已经补齐，但 FE-07 的明确 E2E 退出条件和提醒结果可观察性仍有 2 类缺口；当前仍不能认定 FE-07 开发完成，也不建议作为完成态合入。**

本轮修复仍全部位于未提交工作区；即使以下问题关闭，也必须先形成仅包含 FE-07 相关文件的提交并排除用户原有审查文档改动，再做最终合入审核。

### 已确认关闭的上次问题

- 重新分派会生成原受让人和新受让人的 Mock 通知结果，保留状态/协作者并记录原因、旧/新受让人和时间；详情页可观察通知结果。
- `CivicWorksWorkOrder` 已补齐 `service_request_number`、`external_status_url`、`created_at` 等冻结字段；TRIAGED→ASSIGNED 会创建 Mock 工单，详情页按字段显示工单号、状态、版本、时间和安全过滤后的 HTTP(S) URL。
- CivicWorks Mock 已校验 `previous_status`、`status`、正整数版本与 ISO 时间；provider 单测覆盖非法状态、非法时间、旧版本、重复事件和一次合法 CRM 转换。
- 提醒创建表单可选择 `IN_APP`/`EMAIL`，provider 拒绝其他渠道和非法时间；组件与浏览器测试已验证 `EMAIL` 被实际传入 Mock provider。

### 仍阻塞 FE-07 的问题

1. **[P1] FE-07 浏览器 E2E 仍未证明批量全成全败和外部事件幂等。** 原始 FE-07 指令要求“完成状态机、批量全成全败、提醒生命周期、外部事件幂等和并发冲突 E2E”，`docs/前端开发计划指令.md:111` 还要求观察签名无效、旧版本、重复事件和重试后的单一 CRM 转换。当前 `tools/c311-browser/fe07_matrix.py:63-84` 只执行两条记录的成功批量更新；`263-283` 的失败/重试只选择一条记录，无法证明第二条失败时第一条保持不变。`211-223` 也只发送一次成功 CivicWorks 回调，没有在浏览器中发送旧版本和同一事件的重复/重试并核对状态、历史、审计和写入计数只变化一次。provider 单测虽已覆盖这些分支，但不能替代明确要求的 Mock E2E。应在现有浏览器矩阵的一个稳定角色/视口上补充上述两组实际 provider 行为断言，并让 `test_fe07_matrix.py` 锁定这些证据。
2. **[P2] 提醒创建结果仍未显示收件人和通知渠道。** `client/web/admin/src/views/C311/Staff.vue:132` 的提醒列表只显示标题、状态和到期时间；用户从表单选择 `EMAIL` 或 `IN_APP` 后，页面无法确认本条提醒发给谁、使用哪个渠道。上次复审的明确退出条件是“页面可选择并展示 EMAIL/IN_APP 提醒渠道”，条款 7.8.4 与 9.3.6 又把单一收件人和渠道定义为提醒数据的一部分。应在每条提醒中显示 `recipient_staff_id` 与 `channel`，并在组件测试和浏览器测试中从页面 DOM 验证两种渠道，而不是只通过 `page.evaluate()` 读取 provider 内存。

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，250 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：通过，53 passing。
- `corepack yarn --cwd client/web/admin test:c311`：通过，90 tests。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_*.py'`：通过，22 tests。
- `tools/c311-browser/run-fe07.sh`：通过；5 个角色 × 360/767/768/1023/1024/1440/1920 CSS px 共 35 个主检查全部通过，附加错误场景通过；无意外 console 错误、page error、网络写入或异常响应。开发壳已有的 `custom.css`、`code-snippets.js` GET 500 与外部 websocket 失败仍为允许噪声。
- `git diff --check`：通过；完整差异没有 `server/`、数据库或其他后端文件。
- Codex Security 工作区差异扫描 `9e2d6e1d-215c-4d68-8ce2-b23cb621f5de`：10/10 文件完整覆盖，0 个可报告漏洞；总 token 使用 3,975,473，coverage 为 complete。该结果不解除上述功能验收缺口。

### 再次复审退出条件

- 在浏览器 E2E 中实际制造至少两条记录的批量失败，断言失败请求被指出，且所有选中记录的状态、版本、受让人、优先级、备注、历史和审计均保持不变。
- 在浏览器 E2E 中对 CivicWorks Mock 实际发送非法签名、旧版本和同一 `event_id` 的重复/重试；断言非法/旧版本不改变请求，重复/重试只产生一次合法状态、历史、审计和回调写入。
- 提醒列表显示收件人和 `EMAIL`/`IN_APP` 渠道；组件及浏览器测试直接断言页面可见文本。
- 重跑全部上述门禁与 `git diff --check`；提交只包含 FE-07 相关实现/测试文件，不混入用户原有 `docs/代码审查.md` 或审查输出目录。

---

## 2026-09-04 18:30 CST — FE-07 第四次修复复审

### 审查范围

- 分支：`FE-07`；已提交 HEAD：`b784d733b9abf75c0bae4e13adfb3b448f1bb41e`；基线：`origin/2024.9.x` / `2da61e02979b44e4d4802641fa7532441aecb7e6`。
- 本轮重点复核第三次复审的三个退出条件：多记录批量失败全量不变、CivicWorks 非法签名/旧版本/重复回调 E2E、提醒列表可见收件人和渠道；同时回看完整 FE-07 差异。
- 边界：前端 Mock 独立合入；不要求、不修改、不等待 server 业务逻辑、数据库或真实外部系统。
- 对照：改造技术方案、前后端拆分方案、前端开发计划及条款 7.8.2～7.8.4、7.9.1、7.9.5、10.1～10.4、10.7、11.1、12.2.3。

### 结论

**Approved（本地功能完成态）。当前工作区实现已满足 FE-07 前端 Mock 要求，上轮阻断项全部关闭，没有新的功能或安全阻断项。**

这不是“当前已提交分支可直接合入”的结论：本轮通过审核的 10 个 FE-07 实现/测试修复仍位于 HEAD 之后的未提交工作区。必须先提交并推送这些文件，再以新提交重新确认远端 CI。用户原有 `docs/代码审查.md` 与本目录审查记录不得混入产品提交。

### 上轮问题关闭证据

- `tools/c311-browser/fe07_matrix.py` 在主管场景中执行两条记录的成功批量更新，并在同一真实浏览器 Mock runtime 中提交包含后置失败记录的批量请求；断言返回 `failing_request_id`，成功记录的版本、优先级、受让人和备注不变，批量写入计数不增加。provider 单测另覆盖两个现有记录的部门/重复组不兼容以及后置失败记录场景，均验证所有可读取记录不变。
- 浏览器 E2E 实际调用 `processCivicWorksEvent`，验证非法签名返回 401 且状态不变、旧版本不改变 CRM 状态、同一事件重试返回 duplicate，并核对合法完成只产生一次 IN_PROGRESS、一次 RESOLVED 和一次回调写入。
- 提醒列表直接显示 `recipient_staff_id` 与 `channel`；组件测试和浏览器测试从 DOM 验证 EMAIL 提醒的收件人和渠道，provider 校验仍拒绝不支持渠道。
- 重新分派保持请求状态和协作者，记录原因、旧/新受让人和通知结果；CivicWorks 工单投影包含请求编号、URL、状态、版本和时间；外链仅接受 HTTP(S) 并使用 `noopener noreferrer`。

### 验证结果

- `corepack yarn --cwd lib/js test:unit`：通过，250 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：通过，53 passing。
- `corepack yarn --cwd client/web/admin test:c311`：通过，90 tests。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_*.py'`：通过，22 tests。
- `tools/c311-browser/run-fe07.sh`：通过；5 个角色 × 7 个目标视口共 35 个主检查及附加错误场景全部通过；无未预期 console error、page error、HTTP 失败或网络写入。开发壳允许的静态资源 500 与占位域名 WebSocket 失败仍只作为基线噪声记录。
- `git diff origin/2024.9.x --check`：通过；完整 FE-07 差异不含 `server/`、Go、数据库或其他后端文件。
- Codex Security 工作区差异扫描 `c100efa2-6821-4f34-9b30-c74c8a83616c`：10/10 文件完整覆盖，0 个可报告漏洞，coverage complete；总 token 使用 2,450,267。组织 TAC 状态查询能力在当前环境不可用，已作为覆盖限制记录。

### 提交/合入前条件

- 仅提交本轮 10 个 FE-07 产品/测试文件；不要提交用户原有 `docs/代码审查.md` 和 `docs/c311/`。
- 推送后确认 PR/远端 CI 基于新提交运行且全部通过；若提交内容与本次审核差异一致，即可作为 FE-07 前端 Mock 合入，不需要等待后端。

---

## 2026-09-07 FE-08 再复审结论

- 当前前端 Mock 已实现帮助预览/发布/版本/回滚 UI 与 provider 方法，也为类别更新发送 `If-Match`。
- 仓库冻结契约仍只定义 `admin_help_update`；未定义帮助 preview/publish/versions/rollback operation、path、capability 或 schema。
- `admin_categories_update` 仍未声明 `If-Match`、409 `VERSION_CONFLICT` 或 428 `EXPECTED_VERSION_REQUIRED`。
- 验证通过：lib/js 253 passing / 16 pending；Admin C311 90 passing；Config lint 0 errors（254 warnings）；FE-08 Python 测试 2 passing；FE-08 浏览器矩阵 1440/390 通过。
- 结论：`CONTRACT_BLOCKED / Request Changes`。前端实现质量通过，但新增帮助接口和类别并发协议超出冻结契约，不能作为当前 FE-08 直接合入。需先合入契约变更，或将这些新增能力拆为待契约 PR。

---

## 2026-09-08 FE-09 修复后复审

### 审查目标

- 分支：`FE-09`；HEAD `cf999f5`，工作区包含尚未提交的 FE-09 前端、Mock、类型、fixture、测试和浏览器门禁改动。
- 基线：当前 `origin/2024.9.x@c6881d4`；本地分支落后 7 个主线提交，提交前必须同步并重新跑门禁。
- 范围：工作流、OAuth action、ICS、邮件、报告、审计、联系人邮件导出、CRM 数据导出，以及对应的 C311 provider/mock/types/fixtures/UI/测试。
- 边界：所有前端能力按 Mock-only 审核；不要求、不修改 server 业务逻辑、数据库或迁移。契约以主线当前 `server/compose/types/city311/contract.json` 和 `docs/前端开发计划指令.md:121-131` 为准。

### 自动化证据

- `corepack yarn --cwd lib/js test:unit`：257 passing / 16 pending。
- `corepack yarn --cwd lib/vue test:unit`：53 passing。
- `corepack yarn --cwd client/web/admin test:c311 --runInBand`：94 passing。
- `corepack yarn --cwd client/web/admin lint --no-fix src/views/C311/Extensions.vue`：0 errors、69 warnings。
- `corepack yarn --cwd client/web/admin build`：通过。
- `python3 -m unittest discover -s tools/c311-browser -p 'test_fe09_matrix.py'`：2 passing。
- `C311_ADMIN_PORT=18191 ... tools/c311-browser/run-fe09.sh`：22/22（11 条流程 × 1440/390）通过；未发现未预期页面错误或响应。
- `git diff --check`：通过；当前工作区直接修改没有 `server/` 文件，但分支相对最新主线仍落后 7 个提交。

### 🔴 阻断问题

1. **联系人邮件导出整条前端契约缺失。**
   - 位置：`lib/js/src/311/provider.ts:303-313`、`client/web/admin/src/views/C311/Extensions.vue:435-467`、`lib/js/src/311/fixtures.ts:186-191`。
   - 主线契约已有 `contact_email_export`（`POST /api/v1/staff/contact-email-export`、`contact_email_export` capability、operation response），开发计划明确要求联系人邮件导出且仅输出 verified email、`opt_out=false`。当前没有 Provider 方法、Mock、角色 capability/route、页面入口或成功/失败测试。
   - 风险：FE-09 的管理员导出闭环不可用，合入后无法验收或联调该契约；不是后端未完成问题。
   - 修复：仅在前端 provider/mock/types/fixtures/UI/test 中补齐该 operation、角色范围、字段过滤和结果展示，并覆盖无权限、空结果和成功场景。

2. **ICS 事件没有实现冻结的必需字段和时间规则。**
   - 位置：`lib/js/src/311/mock-provider.ts:132-137,1786-1816`、`client/web/admin/src/views/C311/Extensions.vue:525-528`。
   - 技术方案 7.4.2～7.4.3 要求导入/导出携带 `UID`、`SUMMARY`、`DTSTART`、`DTEND`、`DESCRIPTION`、`STATUS`、重复事件的 `RRULE`、`LAST-MODIFIED`，并区分 UTC `Z` 与 `America/New_York`。当前事件模型只有 UID、摘要、取消标记和 `updated_at`，生成的 ICS 只包含 UID/SUMMARY/STATUS；更新和取消请求同样缺少这些字段。
   - 风险：当前 Mock 通过的只是简化字符串，真实日历交换会丢时间、描述、重复规则和最后修改时间。
   - 修复：扩展前端 Mock 事件/解析器/序列化器和 fixture，校验必需字段、时区、同 UID 原地更新及取消摘要，并补充 provider/浏览器断言。

3. **Workflow OAuth 错误契约未覆盖，缺少 scope 错误语义。**
   - 位置：`lib/js/src/311/enums.ts:202-258`、`lib/js/src/311/mock-provider.ts:1767-1776`、`tools/c311-browser/fe09_matrix.py:91-99`。
   - 契约声明 `INSUFFICIENT_SCOPE`、`INVALID_CLIENT`、`INVALID_TOKEN`、`TEMPORARILY_UNAVAILABLE` 和 `IDEMPOTENCY_CONFLICT`。当前只有通用 `retryable/terminal` 场景；缺少前三个专用 fixture，缺少对应页面/E2E，且无 `workflow.execute` scope 时 Mock 返回 `FORBIDDEN` 而不是 `INSUFFICIENT_SCOPE`。
   - 风险：权限不足、客户端失效和过期 token 在前端无法按契约呈现，错误恢复路径会在 FE-11 首次联调时失效。
   - 修复：新增专用 Mock scenario/错误 payload 和页面断言；无 scope 返回 `INSUFFICIENT_SCOPE`。`C311HttpProvider.executeWorkflowAction` 目前也只发送 `Idempotency-Key`、未提供契约要求的 `Authorization`，应作为 FE-11 real-parity 必须项一并处理，但不需要修改后端。

4. **邮件模板/错误 DTO 与契约不一致。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:286-317,521-524`、`lib/js/src/311/types.ts:662`、`lib/js/src/311/mock-provider.ts:1849`。
   - 页面只有两个静态 `template_id` 选项，选择模板不会加载或编辑模板内容，发送内容始终由纯文本拼接 `<p>`；这不能证明“模板选择/编辑/富文本预览”。同时契约 `mail_delivery.error` 是 nullable error object，类型声明为 `string`，终止失败 Mock 写入字符串，真实响应传入对象后页面会显示 `[object Object]`。
   - 风险：模板选择对用户无实际作用，终止故障信息不可读且类型边界破坏真实 API parity。
   - 修复：增加模板 fixture/provider 读取与编辑、清理后 HTML 预览；将 `error` 改为 `C311ErrorPayload | null` 并按对象的 `message` 展示，补充终止失败断言。

5. **报告目录和构建器未满足最小报告及实体范围。**
   - 位置：`lib/js/src/311/fixtures.ts:370-382`、`client/web/admin/src/views/C311/Extensions.vue:178-229,517`、`lib/js/src/311/mock-provider.ts:1855-1857`。
   - 技术方案 9.8.1 要求五类最小报告（Request volume、Request status and age、Assignment workload、Resolution performance、Follow-up activity）；当前 fixture 只有一个报告。构建器没有实体选择，`saveReport` 永远写入 `service_requests`，因此不支持 `constituents` 和 `follow-up_actions`。
   - 风险：目录和报表实体覆盖不足，后续报告、导出和数据范围验证无法成立。
   - 修复：补齐五类 catalogue fixture 及其 supported fields，增加实体选择并让定义、运行、保存和导出使用所选实体。

6. **报告 CSV 是静态且不符合 RFC 4180 的 Mock 结果。**
   - 位置：`lib/js/src/311/mock-provider.ts:1687-1692`、`client/web/admin/src/views/C311/Extensions.vue:520`。
   - `exportReport` 忽略 report ID、定义和列选择，始终返回硬编码三列；CSV 数据行含裸 `\n` 而不是 CRLF，且没有按实际报告生成字段。计划要求 header、逗号分隔、RFC 4180 quoting、CRLF、UTF-8 和 ISO 8601 时间。
   - 风险：当前浏览器断言只能证明固定样例，不代表用户刚保存的报告可导出；真实下载会出现格式不兼容。
   - 修复：由 Mock operation 根据报告定义生成 RFC 4180 CSV（含 CRLF 和 UTF-8），校验 report 不存在、列和时间字段，并从 operation 结果展示。

7. **审计导出没有应用页面传入的过滤器。**
   - 位置：`client/web/admin/src/views/C311/Extensions.vue:439-442,532-536`、`lib/js/src/311/mock-provider.ts:1870-1887`。
   - 页面只提供 `event_type`、`actor_id`、`occurred_from` 三个字段，契约过滤器还包括 actor/entity/request/source/to 等；更严重的是 `exportAuditEvents(_filters)` 完全忽略参数，operation 的 `exported` 始终为全部审计事件。
   - 风险：用户看到的过滤列表与导出文件范围不一致，可能导出未请求的审计记录，直接违反审计导出语义。
   - 修复：补齐契约过滤控件/序列化和 Mock 过滤逻辑，导出结果数量/内容必须与同一过滤器一致，并覆盖空结果。

8. **CRM 数据导出响应和错误码不符合契约。**
   - 位置：`lib/js/src/311/mock-provider.ts:1890-1898`、`lib/js/src/311/types.ts:694-697`。
   - Mock 只返回极少字段（constituent 只有 ID/email，service request 只有 ID/request number），没有 Chapter 9 要求的完整实体字段/活动自定义字段；非法过滤和 page size 通过 `validation` 返回 `VALIDATION_ERROR`，而 `/api/v1/export/{entity}` 明确要求 422 `INVALID_FILTER`。过滤条件也没有真正筛选结果或证明稳定分页顺序。
   - 风险：Mock 结果无法代表真实导出 schema，前端在联调时会遇到错误码和字段缺失；安全范围测试目前只验证了单条样例。
   - 修复：让 Mock 按实体返回契约字段、应用 entity-relevant filters/updated_since/page token，并对非法过滤返回 `INVALID_FILTER`；增加跨范围、空结果和分页断言。

9. **平台管理员的 capability 与 available route 不一致。**
   - 位置：`lib/js/src/311/fixtures.ts:249-251`、`lib/vue/src/libs/c311-router.ts:63-68`。
   - 平台管理员 capability 已包含 mail/calendar/report 等 FE-09 能力，导航会显示这些入口；但其 `available_routes` 只列 report、audit 和管理路由。点击 `/c311/staff/mail` 或 `/c311/staff/calendar` 会被路由守卫判定无 route 而跳转 403。
   - 风险：角色矩阵中平台管理员无法使用契约声明的授权 staff extension surface，造成 capability 显示与实际访问不一致。
   - 修复：按冻结角色矩阵统一 capability、available route 和导航；为平台管理员直接访问每个扩展路由增加浏览器断言。

### 🟡 建议修改

- `saveWorkflow()` 在编辑任意工作流时固定发送 `active: false`（`Extensions.vue:514`），编辑一个已激活工作流会隐式停用它；应保留当前 active 值，把激活/停用留给显式操作。
- `resolveOperation()` 只查询一次 `getOperation`（`Extensions.vue:510`），真实返回 `PENDING/RUNNING` 时不会继续轮询到最终结果。Mock 可以保持确定性，但 FE-11 前应补充轮询/取消策略。
- `Extensions.vue` 仍有 69 条 lint warning；不阻断当前 CI，但建议在 FE-10 前清理。
- 当前 FE-09 浏览器矩阵只覆盖 1440/390；完整 360、767、768、1023、1024、1920 视口和目标浏览器仍属于 FE-10，不能替代本阶段关键操作证据。

### 已确认没有的问题

- 本轮 FE-09 工作区没有新增 server、数据库或迁移修改，符合前端 Mock-only 边界。
- OAuth、邮件、工作流页面没有硬编码客户端 secret；当前风险是缺少错误/授权 adapter 语义，不是发现了秘密泄露。
- 独立端口运行的 22/22 浏览器流程和现有单元/组件测试通过，说明当前 Mock 自洽，但不能解除上述契约缺口。

### 结论

**Request Changes。FE-09 当前仍未完成，不能提交或合入。** 至少先关闭上述 9 个阻断项，形成仅包含 FE-09 产品/测试文件的提交；然后同步 `origin/2024.9.x`，重新执行全部单元、组件、lint、build、浏览器矩阵和 `git diff --check`。修复过程继续保持前端 Mock-only，不要修改或伪造后端契约。

---

## 2026-09-08 16:00

## FE-09 最新复审（Frontend Mock-only）

### 审查目标

- 类型：本地 `FE-09` 分支/工作区复审。
- 范围：工作流、OAuth action、ICS 日历、邮件、报告、审计、联系人邮件导出和 CRM 数据导出，以及对应 C311 provider、Mock、types、fixtures、UI 和测试。
- 边界：所有前端实现按 Frontend Mock-only 审核；不要求真实 API、数据库或外部系统，不修改后端。Mock 仍必须严格遵守冻结契约和技术方案中的权限、隐私、字段及状态语义。
- 基线：HEAD `cf999f5d465fa55f952c5d14cb1d30c1ae850db4`；`origin/2024.9.x` `c6881d4ee8735436a61a2d78e8ead381fc4f3702`；当前分支落后主线 7 个提交，工作区含未提交 FE-09 改动。

### 自动化证据

- `corepack yarn test:unit`（`lib/js`）：261 passing，16 pending。
- `corepack yarn test:unit`（`lib/vue`）：53 passing。
- `corepack yarn test:c311 --runInBand`（`client/web/admin`）：94 passing。
- `corepack yarn tsc --noEmit`（`lib/js`）：通过。
- `tools/c311-browser/run-fe09.sh`：退出码 0，Mock 浏览器矩阵通过 1440x900 和 390x844 流程。
- `git diff --check`：通过。
- `./node_modules/.bin/eslint src/311/mock-provider.ts`：失败，`mock-provider.ts:311` 有 `no-empty`。
- 全量 `lib/vue`/Admin lint 仍有历史错误，未归因于本轮；但本轮新增 Mock lint 错误本身已足以阻断。

### 严重问题

1. **新增 Mock 代码无法通过 lint。**
   - 位置：`lib/js/src/311/mock-provider.ts:303-312`。
   - `restoreExtensionState()` 使用空 `catch`（第 311 行），定向 ESLint 报 `no-empty`。
   - 风险：若 CI 执行 lib/js lint，FE-09 直接失败；异常也被无提示吞掉，浏览器刷新恢复失败时不可观测。
   - 修复：保留可选存储容错，但在 `catch` 中显式处理/记录或加入简短注释并满足项目 lint；补充损坏 sessionStorage 的测试。

2. **CRM 数据导出存在越权和实体投影缺失。**
   - 位置：`lib/js/src/311/mock-provider.ts:2092-2128`。
   - `service-requests` 和 `constituents` 直接遍历全部 fixture，没有按当前角色部门/地区重新执行记录范围；运行时给部门经理注入 `SANITATION` 请求后仍导出该记录。
   - `follow-up-actions` 永远使用空 source；实体字段不保证契约要求的完整投影/活动自定义字段；`page_size > 100` 返回 `VALIDATION_ERROR`，而 `/api/v1/export/{entity}` 要求 `INVALID_FILTER`；`terminal` 场景返回 endpoint 未声明的 `OPERATION_FAILED`，限流头固定为 `Retry-After: 30` 而不是按契约提供可重试秒数。
   - 风险：跨部门隐私泄露、后续 FE-11 真实联调字段/错误码不兼容，且 follow-up-actions 功能实际不可用。
   - 修复：按角色 scope 过滤所有四类实体，补齐 follow-up-action fixture 和契约投影，绑定并校验 opaque page token/filters/updated_since；非法筛选统一返回 `INVALID_FILTER`，仅使用契约声明的错误状态和 Retry-After。

3. **审计列表/导出未执行记录范围隔离，CSV 丢失必需上下文。**
   - 位置：`lib/js/src/311/mock-provider.ts:2042-2069`。
   - 仅检查 capability，不检查部门经理的 department/district scope；运行时注入 `SANITATION` 审计事件后仍可被经理查询/导出。导出 CSV 仅有 `audit_id,actor_id,event_type,entity_id`，缺少 `actor_type、entity_type、occurred_at、source_channel、before、after`。
   - 风险：审计数据越权和不可追溯，导出文件与 9.7.2/9.7.3 契约不一致。
   - 修复：复用记录范围判定过滤 list/export，输出完整九列 CSV，覆盖跨部门、空结果和过滤后导出一致性测试。

4. **联系人邮件导出违反验证/退订隐私边界。**
   - 位置：`lib/js/src/311/mock-provider.ts:2072-2089`。
   - 只允许 `primary_category/preferred_language/email_opt_out` 三种过滤，缺少契约/后端支持的 `email、department、district`；结果仅返回 `constituent_id` 和 `emails`，不生成 CSV，也不排除未验证邮箱或 `email_opt_out=true` 记录。运行时可看到退订联系人的邮箱。
   - 风险：直接违反只导出 verified 且 `opt_out=false` 的隐私要求，且无法与 operation 下载结果 parity。
   - 修复：按后端已冻结语义建模 verified email、五列 CSV（`email、display_name、primary_category、preferred_language、opt_out`），应用全部过滤和角色 scope，并覆盖退订/未验证/空结果场景。

5. **报告运行、导出和共享没有使用报告定义或权限范围。**
   - 位置：`lib/js/src/311/mock-provider.ts:1780-1808、2032-2039`，`client/web/admin/src/views/C311/Extensions.vue:542-545`。
   - `runReport()` 无论 entity/filters/grouping/sort 都返回固定 service-request 行；`exportReport()` 对 follow-up-actions 返回空，对过滤/排序/分组和用户记录范围不生效。`shareReport()` 只校验角色枚举，未持久化共享关系或限制共享角色不能扩大创建者范围。目录虽有五项，但可运行数据只有一个请求报告。
   - 风险：用户保存的报告与运行/CSV 结果不一致，可能通过分享扩大数据可见范围，后续真实 API 替换会产生严重行为差异。
   - 修复：按定义选择三类实体并应用字段/过滤/分组/最多三列排序，补齐 follow-up fixture 和五类可运行报告；共享关系需持久化、可观察且不得扩大 record scope，CSV 必须来自 operation 结果。

6. **工作流 schema、执行日志和 Idempotency-Key 仍不完整。**
   - 位置：`lib/js/src/311/mock-provider.ts:1825-1867、1883-1904`，`client/web/admin/src/views/C311/Extensions.vue:539-541`。
   - `createWorkflow({})` 可成功写入，未校验契约必需字段；`testWorkflow()` 创建 operation 但不追加执行日志；`executeWorkflowAction()` 未提供 `Idempotency-Key` 仍成功，虽契约将其列为必需 header。UI 保存已有 active workflow 时固定发送 `active:false`，编辑会隐式停用。
   - 风险：非法 workflow 污染 Mock 状态，测试结果不出现在执行日志，重复/无幂等请求与真实服务行为不一致，编辑操作可能意外停用生产流程。
   - 修复：创建/更新前校验完整 definition 和 trigger/conditions/actions；测试 operation 完成后写入 execution log；无 key 返回契约允许的错误并用稳定的请求级 key；保存保留 active 状态，激活/停用只由显式操作完成。

7. **ICS 解析接受不完整事件和任意时区，邮件输入也缺少契约校验。**
   - 位置：`lib/js/src/311/mock-provider.ts:188-223、1907-1964、1967-2005`，`Extensions.vue:546-558`。
   - ICS 只强制 UID/SUMMARY，缺少 `DTSTART/DTEND/DESCRIPTION/STATUS/LAST-MODIFIED` 的完整校验，`TZID=Europe/Paris` 也会接受；UI 更新/取消只发送 UID/SUMMARY/STATUS。`previewMail/sendMail` 不校验收件人、subject、text、附件上限/媒体类型，也不清理 HTML；预览使用 `<pre>` 展示 HTML 源码。邮件幂等 key 仅为 `mail-${subject}`，不同收件人/正文会错误冲突。
   - 风险：日历交换丢失时间/重复/修改时间并接受错误时区；邮件可能发送空内容、绕过 sanitized HTML 边界或把合法不同消息误判为幂等冲突。
   - 修复：强制必需 ICS 字段、UTC `Z`/`America/New_York` 规则并完整保留同 UID 更新/取消元数据；按 `mail_compose` 校验输入、限制附件、返回清理后的预览；为每次 compose 生成稳定且内容绑定的请求级幂等 key。

8. **Mock 固化了契约未声明的异步错误状态。**
   - 位置：`lib/js/src/311/mock-provider.ts:95-137、1907-1915、1992-2000、2095-2099`。
   - `calendar_import` 的 retryable/terminal、`mail_delivery_get` 的 503 `TEMPORARILY_UNAVAILABLE` 和 `data_export` 的 500 `OPERATION_FAILED` 都不在对应冻结 endpoint 的错误状态集合中；当前 `failIfNeeded()` 还把限流 Retry-After 固定为 30。
   - 风险：前端测试通过了真实契约不可能返回的状态，FE-11 parity 时错误处理分支会失效。
   - 修复：删除这些 endpoint 的未声明 scenario，或先由契约负责人冻结对应错误；在契约未变更前仅使用已声明错误/operation 内失败表示，并同步更新浏览器矩阵。

### 建议修改

- `resolveOperation()` 只查询一次 `getOperation()`（`Extensions.vue:535`）；真实服务返回 `PENDING/RUNNING` 时不会轮询到最终结果。FE-11 前补充轮询、退避、取消和刷新恢复策略。
- 当前 FE-09 浏览器矩阵只覆盖 1440x900 与 390x844；360、767、768、1023、1024、1440、1920 全部边界和 Edge/Chrome/Firefox/Safari 属于 FE-10，但不能替代本阶段关键契约场景。
- 全量 `lib/vue` 和 Admin lint 仍有历史 error，应在 CI 规则允许的前提下单独跟踪；不要将其与本轮 `mock-provider.ts:311` 新增 error 混淆。

### 已确认

- 本轮未修改 `server/`、数据库、迁移或后端业务，符合用户的 Frontend Mock-only 约束。
- 未发现 OAuth、邮件或工作流秘密写入前端；OAuth 真实 client-credentials/Authorization header parity 可放到 FE-11，但 Mock 必须先遵守已冻结 header 和错误语义。
- 单元、组件、TypeScript、构建和浏览器门禁通过只说明当前自定义 Mock 流程自洽，不能解除上述契约、隐私、权限和状态问题。

### 结论

**Request Changes：FE-09 仍未完成，不能提交或合入。** 当前至少有 8 项阻断问题。请只在前端 Mock/provider/types/fixtures/UI/test 中修复，随后同步 `origin/2024.9.x`（当前落后 7 个提交），重跑单元、组件、定向及全量 lint、TypeScript、构建、FE-09 浏览器矩阵和 `git diff --check`，再申请复审。后端无需等待或修改，但不能用未冻结 endpoint/错误状态替代前端实现。

---

## 2026-09-09 10:45

## FE-09 最终复审（Frontend Mock-only）

### 审查目标

- 类型：本地 `FE-09` 分支/工作区复审。
- 范围：工作流及 OAuth action、ICS 日历、邮件、报告、审计、联系人邮件导出、CRM 数据导出，以及对应 provider、Mock、types、fixtures、UI、组件测试和浏览器门禁。
- 边界：按用户约束，当前所有前端能力均为 Frontend Mock-only；不要求真实 API、数据库或外部系统，不修改后端。Mock 仍需遵守冻结契约的字段、权限、错误码、幂等和范围语义。
- 当前状态：分支 `FE-09`，HEAD 与 `origin/2024.9.x` 均为 `c6881d4`；FE-09 改动仍在未提交工作区。

### 审查输入证据

- `corepack yarn test:unit`（`lib/js`）：274 passing，16 pending。
- `corepack yarn test:unit`（`lib/vue`）：53 passing。
- `corepack yarn test:c311 --runInBand`（Admin）：96 passing。
- `corepack yarn tsc --noEmit`（`lib/js`）：通过。
- FE-09 Playwright Mock 矩阵：52/52 通过，覆盖 workflow designer、department manager、platform administrator、constituent 角色及成功/失败/冲突/分页/刷新场景，视口 1440x900、390x844。
- `python3 -m unittest test_fe09_matrix.py`：2 passing。
- `lib/js`、`lib/vue`、Admin production build：完成；仅有既有 lint/deprecation/asset-size warning。
- 本轮改动文件定向 ESLint：通过；`git diff --check`：通过。
- 改动文件未包含 `server/`、数据库或迁移目录。

### 问题清单

#### 🔴 严重问题

无。上一轮阻断项已关闭：报告目录及实体投影、日期过滤和 CSV 生成，审计过滤/分页 token/完整导出字段，CRM 导出范围/错误码/分页，联系人邮箱 verified 与退订过滤，工作流 schema/执行日志/幂等，OAuth 错误场景，ICS 必需字段/时区/UID 更新取消，邮件清理/附件限制/SMTP 生命周期，角色 capability 与路由一致性均有实现和测试证据。

#### 🟡 建议修改

1. `C311HttpProvider.executeWorkflowAction()` 默认只带调用方传入的 headers，不会自动提供契约要求的 OAuth `Authorization`；`exportData()` 也没有单独的请求头参数。Mock-only 阶段不阻断，FE-11 真实联调前必须由认证 transport 明确注入 bearer/client-credentials，并补 parity 测试。
2. `DataExportQuery` 继承 `ListQuery`，因此类型和 HTTP provider 会接受/序列化契约未定义的 `sort` 参数。当前 UI 不发送该参数；建议在 FE-11 前收窄类型或在 provider 层拒绝，避免真实 API 联调时静默忽略。
3. Mock 审计列表允许 `audit_id` 排序，而冻结契约的允许排序字段不含 `audit_id`；当前 UI 未暴露此选项。建议收敛 Mock 校验并补契约回归测试。
4. `.github/workflows/test.yml` 当前显式执行到 FE-07，尚未接入 `run-fe09.sh` 及 FE-09 artifact；本地门禁已通过，但 CI 不会自动复跑 FE-09 浏览器矩阵，建议在后续 CI 变更中补上。
5. 全量 Admin/lib/vue lint 仍报告未改文件的既有错误/警告；本轮变更文件定向 lint 已通过。不要把既有 lint 债务与 FE-09 功能结论混淆，后续可单独治理。

### 结论

**Approved（仅限 Frontend Mock-only 合入）**。

- 严重阻断：0。
- FE-09 前端 Mock 关键流程和错误/权限/隐私边界已有自动化证据，可以进入提交和合入流程。
- 当前工作区尚未提交、尚未推送；提交时只纳入 FE-09 产品、provider/mock、fixture、类型、测试和门禁文件，审查记录可单独提交，避免混入无关改动。
- 本结论不等价于真实 API、OAuth、邮件 SMTP、工作流外部动作、数据库持久化或跨服务一致性已上线；这些保留到 FE-11 真实联调验收。

---

## 2026-09-09

## FE-10 质量加固复审（Frontend Mock-only）

### 审查范围

- 分支：`FE-10`。
- HEAD：`bdab655 Harden FE-10 frontend quality`；基线：`origin/2024.9.x@5dab35a`；分支领先基线 1 个提交。
- 范围：FE-10 跨页面质量加固、响应式/浏览器矩阵、WCAG 2.2 AA 旅程、路由/焦点/状态/语言/刷新回归、对应 Mock、fixture、测试和 CI 门禁。
- 边界：本次按 Frontend Mock-only 审核；未要求真实 API、数据库或外部系统，不修改 `server/`、数据库或迁移。Mock 仍必须遵守冻结契约的字段、权限、错误和状态语义。

### 已通过检查

- `lib/vue` 单元测试：56 passing。
- Compose C311 组件测试：100 passing。
- Admin C311 组件测试：100 passing。
- `lib/js` TypeScript 和单元测试：通过。
- `PYTHONPATH=tools/c311-browser python3 -m unittest discover -s tools/c311-browser -p 'test_fe10_matrix.py'`：2 passing。
- FE-10 改动文件定向检查：Admin 0 error（3 warnings），`git diff --check 5dab35a..bdab655` 通过。
- 本提交未修改后端业务逻辑、数据库或迁移文件。

### 阻断项

#### 🔴 严重

1. **浏览器矩阵没有覆盖 FE-10 要求的边界和目标浏览器。**
   - `tools/c311-browser/fe10_matrix.py` 的 `VIEWPORTS` 使用了 `390`，遗漏必测的 `767` 和 `1023`；静态测试还把这个错误矩阵锁定为通过。
   - `BROWSERS` 常量未被使用，脚本每次只读取一个 `C311_BROWSER` 并启动一个引擎；当前 CI 只安装 Chromium，也没有 Edge、Chrome、Firefox、Safari 的实际运行证据。`.github/workflows/test.yml` 未接入 `run-fe10.sh` 或 FE-10 artifact。
   - 修复：将矩阵改为至少 `360、767、768、1023、1024、1440、1920` CSS 像素；为 Chrome/Edge/Firefox/WebKit（Safari 代理）分别运行并保存结果；将 FE-10 脚本和 artifact 接入 CI，静态测试只接受正确矩阵和多浏览器执行。

2. **四条 FE-10 WCAG 旅程只是浅层 smoke，未完成质量验收闭环。**
   - `login_identity()` 未提交登录、验证失败恢复和完整键盘路径；`public_submit()` 未断言成功返回 `request_number` 与 `SUBMITTED`；`anonymous_lookup()` 未验证凭据不匹配时统一“未找到”且详情为空、状态文本/API 值稳定；`staff_update()` 未执行员工更新。
   - 矩阵也没有覆盖可见焦点、错误标签关联、模态/帮助抽屉焦点、异步播报、语言英文回退和未保存离开保护等 FE-10 要求。
   - 修复：四条旅程分别执行真实 Mock 提交/更新和失败分支，断言状态、焦点、ARIA 关联、播报、语言切换、刷新恢复和离开保护，并在每个目标浏览器/视口留下截图与 JSON 日志。

3. **FE-10 浏览器门禁实际失败，仓库没有可审计的质量报告。**
   - 执行 `C311_ARTIFACT_DIR=/tmp/c311-fe10-review-1788940178 tools/c311-browser/run-fe10.sh` 收到大量 `500 Internal Server Error` 和外部 WebSocket 失败，最终因 `console_errors` 非空失败。
   - 产物仅在临时目录，仓库中没有 FE-10 响应式、无障碍、语言、浏览器兼容和刷新回归报告、截图或日志，因此无法证明 FE-10 退出条件已满足。
   - 修复：明确 Mock 运行依赖，消除非预期 console/page/request 错误；将通过后的矩阵 JSON、截图、日志和已知限制作为 PR artifact/记录提交，并在 CI 中可重复执行。

4. **FE-10 新增代码存在 ESLint error。**
   - `client/web/compose/src/views/C311/Portal.vue:1002` 使用 `void error`，触发 `no-void`；该错误来自本次提交，不是历史债务。
   - 修复：删除无用的 `catch` 变量或改为不触发 `no-void` 的处理，并重新运行 Compose 改动文件定向 lint。

#### 🟠 合入后软件风险（需在合入前关闭或明确契约处理）

5. **富文本安全标记与冻结契约不一致。** `PublicPortal.vue` 只有响应带 `sanitized: true` 才渲染 `v-html`，但冻结契约的 `content_object.body`/`help_content.body` 仅声明 `format: sanitized_html`；`C311HttpProvider` 直接返回响应，不补充 marker。合法 HTTP 响应可能被前端误判为不安全并显示 422/空内容。应由前端 provider 按冻结 `sanitized_html` 映射可信本地标记，或先扩展并冻结契约后同步实现；Mock 不得用额外字段掩盖 HTTP parity 问题。

6. **RFC 6901 字段错误路径不能稳定聚焦真实控件。** `Portal.vue` 的 `formFieldTargets` 使用点号键，`C311ErrorSummary.vue` 仅做斜杠归一化；如 `/requester/display_name`、`addresses/0`、`custom_fields/...` 可能找不到实际控件，违反错误摘要关联和焦点移动要求。应建立完整 JSON Pointer 到控件 ID 的映射，并覆盖嵌套对象、数组和自定义字段测试。

### 结论

**Request Changes：FE-10 尚未完成，当前不能直接提交或合入。**

前端 Mock 的单元、组件、TypeScript 和部分静态门禁已通过，但这只能证明局部实现自洽，不能替代 FE-10 要求的跨视口、四条 WCAG 旅程、四浏览器运行和可审计证据。请仅在前端、Mock、fixture、测试和 CI 门禁范围内修复上述问题，不修改后端；修复后重跑定向/全量 lint（至少确保本提交无 error）、单元、组件、TypeScript、构建、完整 FE-10 浏览器矩阵和 `git diff --check`，再申请复审。

### 工作区记录

- 审查时 `git status --short --branch`：`FE-10...origin/2024.9.x [ahead 1]`；审查记录文件已有修改，`docs/c311/` 为审查记录目录，不应误认为产品代码。

---

## 2026-09-09

## FE-10 阻断修复后复审（Frontend Mock-only）

### 审查目标

- 分支：`FE-10`。
- HEAD：`b3f7e82 Harden FE-10 browser quality gate`；基线：`origin/2024.9.x@5dab35a`；分支领先 2 个提交。
- 本轮只复核上一轮 FE-10 阻断修复，不扩展到后端、数据库或真实外部系统。所有前端能力继续按 Frontend Mock-only 审核。

### 自动化与实际运行证据

- `lib/js`：TypeScript 通过，单元测试 `278 passing, 16 pending`。
- `lib/vue` 单元测试：`57 passing`；Admin C311 组件测试：`101 passing`。
- `PYTHONPATH=tools/c311-browser python3 -m unittest discover -s tools/c311-browser -p 'test_fe10_matrix.py'`：`2 passing`。
- `lib/js`、`lib/vue`、Compose、Admin production build：通过。
- Compose/Admin 标准 lint：`0 error`，仅有既有格式 warnings；FE-10 修改的 lib/vue 文件定向 lint：`0 error`。
- `git diff --check 5dab35a..HEAD`：通过；改动文件没有 `server/`、数据库或迁移目录。
- 按 CI 顺序执行本地 C311 包构建安装后，`C311_BROWSER=all tools/c311-browser/run-fe10.sh`：`42` 项通过（Chromium、Firefox、WebKit × 7 个视口 × Compose/Admin），`console_errors=0`、`page_errors=0`、`failed_requests=0`、`write_requests=0`，产出矩阵 JSON 和截图。

### 已关闭问题

- 视口常量已覆盖 `360、767、768、1023、1024、1440、1920`。
- 浏览器脚本已执行 Chromium、Firefox、WebKit，CI 已安装 Firefox/WebKit 并上传 FE-10 artifact。
- 登录失败/忘记密码、请求草稿刷新与离开保护、匿名查询、提交成功 `request_number`/`SUBMITTED`、员工 TRIAGED 更新、焦点/ARIA 标记和 secret-shaped storage 检查均已纳入 Mock 矩阵。
- HTTP provider 已按冻结 `sanitized_html` 响应格式补充本地安全 marker；RFC 6901 路径归一化和动态电话/地址控件映射已有实现与单元覆盖。

### 阻断项

#### 🔴 严重

1. **没有真正执行 FE-10 要求的 Edge 和 Safari 目标浏览器。**
   - `tools/c311-browser/fe10_matrix.py:29` 的 `BROWSERS` 只有 `chromium、firefox、webkit`；`.github/workflows/test.yml:39` 也只安装这三类引擎。
   - Chromium 可作为 Chrome 引擎，WebKit 可作为 Safari 近似代理，但两者都不能证明 Edge 浏览器行为，也不等价于 macOS Safari；脚本注释中的 “Edge-compatible Chromium” 不是实际 Edge 运行证据。
   - 修复：在 CI 安装并实际运行 Edge（Playwright Edge channel 或固定 Edge 二进制），并将 Safari 的运行方式/限制明确记录；矩阵结果必须分别标记 Chrome、Edge、Firefox、Safari。

2. **“逐页验证”仍未覆盖全部页面、全部视口和全部质量项。**
   - `fe10_matrix.py:225-253` 只在所有视口打开 Compose 入口和 Admin 员工入口；`extension_smoke()` 只在宽度 `767`、`1440` 运行，并且只点击一个 action，不验证这些页面的完整键盘路径、错误摘要、模态/帮助抽屉焦点和刷新恢复。
   - 矩阵没有实际切换并断言 `EN/ES/VI` 与英文回退，也没有验证语言切换后的路由/有效输入保持；当前的 locale request 隔离只能证明没有外部网络噪声。
   - 修复：为所有 FE-10 涉及路由按 `360、767、768、1023、1024、1440、1920` 和目标浏览器执行页面级检查；补语言切换/缺失 key 回退、帮助抽屉与模态焦点封闭/返回、异步播报、未保存离开和刷新恢复断言，并上传对应证据。

### 结论

**Request Changes：FE-10 仍未完成，当前不能直接提交或合入。**

上一轮主要实现和本地 CI 级 Mock 矩阵已通过，当前剩余阻断集中在 FE-10 明确要求的浏览器真实性和逐页质量证据，不涉及后端修改。若项目负责人正式把 Chromium/WebKit 代理认可为 Edge/Safari 等价测试，并接受 extension smoke 的范围，则可将本轮结论放宽为 Mock-only 条件批准；按现有 FE-10 原文的严格口径，仍应先补齐上述两项。

### 工作区记录

- 审查时分支为 `FE-10...origin/2024.9.x [ahead 2]`；工作区仅有既有审查文档修改和 `docs/c311/` 审查记录，不应误认为产品代码变更。

---

## 2026-09-10

## FE-10 阻断修复后复审（Frontend Mock-only）

### 审查范围

- 分支：`FE-10`；HEAD：`6d95049 Expand FE-10 browser quality matrix`；基线：`origin/2024.9.x@5dab35a`，领先 3 个提交。
- 范围：FE-10 跨页面质量加固、响应式/无障碍/语言/刷新门禁、四条 WCAG 旅程、扩展页面和 CI 证据。
- 边界：本轮所有前端能力继续按 Frontend Mock-only 审核；未修改 `server/`、数据库、迁移或后端业务逻辑。

### 已执行并通过

- `PYTHONPATH=tools/c311-browser python3 -m unittest discover -s tools/c311-browser -p 'test_fe10_matrix.py'`：3 passing。
- `lib/js` TypeScript：通过；单元测试：278 passing、16 pending。
- `lib/vue` 单元测试：57 passing；Admin C311 组件测试：101 passing。
- Compose/Admin production build：均通过。
- Compose/Admin lint：均为 0 error（仅既有格式/依赖 warnings）。全量 `lib/vue` lint 仍有基线中的 `CUploader.vue:142` `no-this-alias`，不是 FE-10 新增。
- `C311_BROWSER=all C311_ARTIFACT_DIR=/tmp/c311-fe10-review-latest tools/c311-browser/run-fe10.sh`：42/42 通过，覆盖 Chromium、Firefox、WebKit；7 个视口 `360、767、768、1023、1024、1440、1920`；Compose/Admin 均通过；`console_errors=0`、`page_errors=0`、`failed_requests=0`、`write_requests=0`。产物包含 `fe10-matrix.json` 和截图。
- `git diff --check 5dab35a..HEAD`：通过；改动文件不包含后端、数据库或迁移目录。

### 已关闭的上一轮问题

- 扩展页面已在全部 7 个视口执行，并覆盖 workflow/report/mail/calendar/audit 页面 action/result 检查。
- 四条旅程已包含登录失败与恢复、提交成功状态、匿名查询、员工 TRIAGED 更新、草稿刷新/离开保护、语言切换与刷新、错误摘要、状态播报、帮助抽屉和模态焦点返回。
- CI 已安装并运行 Chromium、Firefox、WebKit，且上传 FE-10 artifact；Mock 网络边界无业务写请求。

### 🔴 阻断项

1. **目标浏览器矩阵仍不满足 FE-10 原文的 Edge/Chrome/Firefox/Safari 要求。**
   - `tools/c311-browser/fe10_matrix.py:27-29` 只声明 `chromium、firefox、webkit`；`.github/workflows/test.yml:39` 也只安装这三种 Playwright 引擎。
   - Chromium 只能作为 Chrome/Edge 兼容内核代理，WebKit 只能作为 Safari 行为近似，当前没有实际 Edge 或 Safari 浏览器运行和分别标记的证据。提交说明也明确承认 native Edge/Safari 未单独启动。
   - 这不是后端依赖，但按 FE-10 的严格退出条件仍不能宣称四个目标浏览器全部通过。应在 CI 或联合验收环境补真实 Edge 与 Safari 运行记录；若项目负责人书面接受 Chromium/WebKit 代理，需在 PR 中将其记录为条件豁免而不是“全部目标浏览器通过”。

### 🟡 非阻断证据/后续建议

- `fe10_matrix.py:72-87` 检查可访问名称和焦点落点，但未断言 CSS `:focus-visible` 的可见样式；`107-123` 的语言检查也未主动注入缺失 key 来证明英文回退；矩阵没有直接断言 ET 日期字符串或富文本恶意标记在浏览器中不执行。现有单元/组件测试覆盖了时间格式和安全 marker，这些属于验收证据增强项，建议在 FE-11 前补浏览器断言或人工验收记录。
- 全量 `lib/vue` lint 的 `CUploader.vue:142` 是基线错误；本轮改动相关检查和构建均未发现新的 error，不应借 FE-10 合入顺手修改无关历史代码。

### 软件层面风险

- 合入不会改变后端契约、数据库或服务端状态机；Front-end Mock-only 范围内未发现新的业务写入、秘密泄露或构建失败风险。
- 未覆盖真实 Edge/Safari 会留下浏览器兼容回归风险；未补上述浏览器级证据会使焦点可见性、英文回退、ET 展示和富文本执行安全只能依赖单元/组件测试。真实 API、服务端清理、持久化和跨链路一致性仍属于 FE-11。

### 结论

**Request Changes（严格 FE-10 口径，暂不能直接提交或合入）。**

FE-10 的 Mock 功能和自动化矩阵已基本完成，上一轮的扩展页、视口、语言、焦点和错误门禁问题已关闭；唯一严格阻断是没有实际 Edge 和 Safari 运行证据。若负责人正式批准 Chromium/WebKit 作为等价代理并在 PR 中记录豁免，则可改为 **Approved（Frontend Mock-only，条件批准）**，不需要等待或修改后端。

### 工作区记录

- 审查时：`FE-10...origin/2024.9.x [ahead 3]`；工作区仅有既有审查文档修改和 `docs/c311/` 审查记录，未发现产品后端变更。

---

## 2026-09-10

## FE-10 浏览器代理豁免确认

- 项目负责人已明确接受 Playwright Chromium 作为 Chrome/Edge 兼容内核代理、Playwright WebKit 作为 Safari 行为代理。
- 本豁免仅适用于当前 Frontend Mock-only 的 FE-10 合入，不得表述为真实 Edge/Safari 已运行通过；`fe10-matrix.json` 中的实际运行引擎仍须如实记录为 Chromium、Firefox、WebKit。
- FE-10 条件批准：现有 42/42 Mock 矩阵、7 个视口、Compose/Admin、四条关键旅程及质量检查通过；真实浏览器差异、真实 API、服务端清理、持久化和跨链路一致性保留到 FE-11。
- 该接受不要求后端修改，不改变冻结契约，也不授权跳过后续 FE-11 的真实浏览器/API 验收。

### 结论

**Approved（Frontend Mock-only，条件批准）。** 代理浏览器限制已由负责人接受并作为本次提交和 PR 的明确已知限制记录。

---
