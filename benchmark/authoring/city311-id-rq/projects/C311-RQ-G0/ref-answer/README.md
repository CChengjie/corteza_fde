# Hidden Reference Answer

The materialized answer directory contains the complete runnable City 311
reference source. It is produced from the frozen reference commit recorded in
`../task.yaml`, never supplied to an agent, and verified with the same fixture
and validator bundle as an agent submission.

The answer includes the G0 request lifecycle, mapping boundary, persistent
attachments, public-status privacy projection, staff scope controls,
idempotency, audit entries, and restart-safe runtime configuration.
