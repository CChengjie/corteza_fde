# P1: Platform Bootstrap

Deliver the first runnable customer-system increment from the supplied empty
build harness. The service must build in Docker, load runtime configuration,
expose `/healthz`, and expose a non-secret system information endpoint.
