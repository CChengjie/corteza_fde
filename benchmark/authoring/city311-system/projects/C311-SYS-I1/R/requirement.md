# I1: Integration Boundary

Extend D1 with a server-side mapping adapter. It must call the supplied mapping
fixture with its server credential and expose a typed validation result to the
customer system. No City311 request creation is required at this node.
