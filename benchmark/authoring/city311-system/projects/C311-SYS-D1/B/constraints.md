# Constraints

Preserve P1 build, configuration, and health behavior. Request numbers are
public identifiers, never database IDs. The API contract is versioned and
unknown domain values must not be silently accepted.
